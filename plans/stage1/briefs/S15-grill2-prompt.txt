Read GROK.md at the repository root first. You are the focused RE-GRILL for the S1.5 terminal bridge fix (git log -1, git diff HEAD~1; reports plans/stage1/reports/S15-turnsb.md and the rejection S15-grill.md). Verify the three hard blockers are closed by attack: (1) restart mid-turn - read RunningTurnSurvivesSiloRestartAndCompletes: does it assert TERMINAL head + queue progress (not existence)? trace ReconcileActiveExecutionAsync for a hole (execution Waiting forever - does anything resolve it? DelayDeactivation interplay); (2) cancel-of-running - trace the interleaving: BeginAccept fire-and-return + DispatchWorkerCancel - can a completed result still append AFTER Cancelled applied (race both ways is honest ONLY if no double-reply and queue advances exactly once - verify); can Cancelling head deadlock if execution never terminalizes? (3) worker death - liveness reminder + FailAbandonedRunningIfNeededAsync: what is the abandonment deadline, is it durable, does it fire after full silo restart (reminder re-registration!), and does KilledWorkerReachesFailedAndQueueAdvances really kill (not just complete) the worker? (4) the bridge itself - ExecutionTerminal is directed: trap 2 check (origin grain gone/renamed -> notification lost - what recovers? activation reconcile must cover it), replay/dedupe of the wake-up (at-least-once delivery - is re-apply idempotent by ExecutionId+Revision?), and can a forged ExecutionTerminal fired by a hostile client neuron trick chat (it re-reads Execution - confirm it NEVER trusts payload text/result without the Read)? (5) allow-list - registry composition: can a module register 'worker' for a malicious grain type at runtime, is refusal settled? (6) regression sweep - 147 tests: any prior proof weakened/deleted to make this pass (git diff on test files)? Verify gate yourself (build + test exe; ChartVocabularyProofs single timeout = known ticketed flake). Judge only; change NOTHING; no git writes. FINAL action: write plans/stage1/reports/S15-grill2.md ending with VERDICT: APPROVE or VERDICT: REJECT + findings file:line severity.
