Read GROK.md at the repository root first. You are the GRILL session for the S1.5 durable-turns seam (last commit - git log -1, git diff HEAD~1; report plans/stage1/reports/S15-turns.md; brief plans/stage1/briefs/S15-turns.md). This is the deepest product seam - attack hard: (1) kernel trap 1 - anywhere the chat turn pipeline awaits an effect of its own Send without FlushOutboxAsync (deadlock-by-starvation); trap 2 - new facts (chat.turn-lifecycle) emitted with zero receivers get silently lost - who consumes them, is every visible thing route-confirmed; trap 8 - did any new IHandle<T> join the broadcast catalog and spawn ghosts per emit? (2) FIFO integrity - two rapid sends racing the queue head, cancel-of-head racing completion, duplicate CommandId dedupe still holding, queue state surviving restart with a Running head; (3) the Execution adapter - does ChatTurnWorker honor the OutcomeUncertain invariant for the AI call (model call is non-idempotent-ish but retryable - what did they decide and is it defensible?), does worker failure produce a durable Failed turn (never a stuck Running)? (4) WorkerDispatchRelayNeuron change - the relay now allows domain worker grain types: can an arbitrary grain type be dispatched (security/typing hole) or is it constrained? (5) observer semantics - does the POST observer leak (unbounded journal watch, missed disposal on abort), does reconnect truly reconstruct queued/running/completed from events alone? (6) P0-6 - safe-point persistence: what exactly persists per tool round, can a crash between rounds double-execute a tool on resume? (7) actor refusal - settled, and scripting samples stamped? (8) contract hygiene - additive-only event changes (older client with unknown ), wire aliases untouched, no MAF types leaked outside AI. Verify gate yourself (build + test exe; ChartVocabularyProofs single timeout = known ticketed flake). Judge only; change NOTHING; no git writes. FINAL action: write plans/stage1/reports/S15-grill.md ending with VERDICT: APPROVE or VERDICT: REJECT + findings file:line severity.
