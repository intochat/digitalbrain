// Stub host so AppHost can reference DigitalBrain.Scripting later.
// Wire authenticated IPC + Sdk lease channel here; do not load this process from Kernel.
Console.WriteLine("DigitalBrain Scripting host — wire IPC later");
return 0;
