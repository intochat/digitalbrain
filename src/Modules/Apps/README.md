# Apps

App manifests, install, versions and Save as app.

Skeleton created by the orchestration seams (W0). Contracts under `Contracts/` are frozen for the parallel run; request changes through the orchestrator.
