name: digitalbrain_corev2
description: Authority-neutral DigitalBrain CoreV2 product protocol client.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.2

# Add regular dependencies here.
dependencies:
  http: ^1.4.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
