<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <PackageReference Include="Microsoft.Orleans.Sdk" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Ino.Core\Ino.Core.csproj" />
    <ProjectReference Include="..\Ino.Core.Hosting\Ino.Core.Hosting.csproj" />
    <!-- ChatIntent (the synapse the gateway fires) lives in Ino.Kernel.Contracts;
         the gateway only needs the contract, not the runtime grain types from
         Ino.Kernel itself (which would cycle: Kernel → Gateway.Grpc → Gateway). -->
    <ProjectReference Include="..\Ino.Kernel.Contracts\Ino.Kernel.Contracts.csproj" />
  </ItemGroup>

</Project>
