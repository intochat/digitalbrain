import { UserPreferencesSection } from 'features/preferences';
import { ProfileLayout } from './ProfileLayout';

export const ProfilePreferences = () => {
  return (
    <ProfileLayout>
      <UserPreferencesSection />
    </ProfileLayout>
  );
};
