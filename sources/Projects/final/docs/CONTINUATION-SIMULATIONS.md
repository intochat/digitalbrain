You are a senior C# / Microsoft Orleans / Aspire / testing-infrastructure expert working on **DigitalBrain** at `E:\Projects\final` (.NET 11 preview 4 SDK `11.0.100-preview.4`, Orleans `10.1.1-preview.1` + Journaling alpha, Aspire `13.4.x`, Hex1b 0.164.1, Reqnroll BDD, xunit v3 + Microsoft.Testing.Platform, Playwright 1.60). Use context7 and microsoft-learn MCPs for every API before writing code — especially the **current xunit v3 / MTP command-line filter and report switches** (do NOT guess switch names), Reqnroll tag→trait generation for xunit.v3, `Aspire.Hosting.Testing` lifecycle, and Playwright .NET. Code rules: no comments (self-documenting only), production-ready, `[GenerateSerializer]` + `[Id(n)]` discipline, concrete arrays on serialized types (never collection expressions into `IReadOnlyList<T>`), no `GrainFactory` in constructors, latest packages central in `Directory.Packages.props`, relative paths inside `final/`. Process is The 5 Steps — Steps 1–2 are already executed and recorded in `docs/SIMULATION-PLAN.md`; this session implements Steps 3–5 as stages SIM0–SIM4. Do NOT re-litigate the plan; flag (and stop on) anything that contradicts it.

## Read first (mandatory, in this order)
1. `docs/SIMULATION-PLAN.md` — the authoritative spec: engine/doors architecture, synapse shapes, filter grammar, deletions, stages, gates, landmines.
2. `src/DigitalBrain.Core.Tests/Simulation.cs` (the factory with hidden suites), `DistributionSimulationBindings.cs`, `DistributionDynamicHandlers.feature`, `GoogleAuthU4.feature`, `NeuronE2ETest.cs`, `InoTests.cs`, `ProjectReviewTests.cs`, `DigitalBrain.Core.Tests.csproj`.
3. `run-ci.ps1`, `ino.cs`, `src/DigitalBrain.Kernel/Experiences/RuleHostNeuron.cs` (ReplayObservedSynapsesAsync + RuleReplayReport), `src/DigitalBrain.Kernel/Experiences/MarketplaceNeuron.cs` (quarantine path), `src/DigitalBrain.Core/Application/SimulationNeuron.cs`.
4. `docs/UNIFICATION-PLAN.md` §6 + `docs/CONTINUATION-UNIFICATION.md` completion section (what U0–U5 landed; the AppHost project survives as the Aspire.Hosting.Testing target — do not delete it).

## First actions (mandatory, no exceptions)
1. `git status` — clean tree or stop.
2. `.\run-ci.ps1` — green baseline or stop.
3. `dotnet test src/DigitalBrain.Core.Tests/DigitalBrain.Core.Tests.csproj --filter "FullyQualifiedName~DistributionDynamicHandlers" --logger "console;verbosity=minimal"` — record passing count AND wall time (the SIM1 acceleration claim needs this baseline). This exact FQN filter must be 0 failures after EVERY stage — it is the Core Law gate and its contract never changes.
4. Verify via microsoft-learn/context7: xunit v3 MTP executable filter switches (trait/class/method/query), machine-readable report output switch (TRX/JSON), Reqnroll `@tag` → xunit trait mapping for the xunit.v3 adapter. Record findings in SIMULATION-PLAN.md §SIM0 before writing simulation.cs.

## Owner answers SD1–SD4 (defaults pre-approved by Vlad; edit only if instructed)
- **SD1 (dev-mode compiled-suite spawn from the brain):** YES, behind the capability grant. `SimulationHostNeuron` may spawn the MTP test exe with the mapped filter on dev machines; the spawn is privileged, routed through the D3 grant surface, journaled, never silent.
- **SD2 (NeuronE2ETest fate):** REPLACE. Extract the harness into `SimulationUiHost`; rewrite the assertions as `@Ui` scenarios that Skip-with-reason on missing prerequisites and genuinely fail otherwise; delete NeuronE2ETest; list it in DELETED.md.
- **SD3 (watch-mode browser):** Playwright headed Chromium when `playwright install` has run (detect, don't assume), plain OS URL launch as the documented fallback; the active mode is printed in the run output, never silent.
- **SD4 (report persistence):** BOTH. `SimulationReport` synapse on the timeline is the truth; JSON + screenshots under `pa-files/simulations/{runId}/` are projections (same rule as OTel).

## Stages (one commit per stage; commit message lists deletions; stop cleanly at any stage boundary if gates degrade)

**SIM0 — verify + measure (no product code).** Record in SIMULATION-PLAN.md: the verified MTP/xunit-v3 switch names, the Reqnroll tag→trait behavior, and the baseline high-sev wall time from first-action 3.

**SIM1 — factory cleanup + tagging (the Step-2 deletions).**
- `Simulation.StartAsync` returns `(Cluster, Grains, Brain)` and NOTHING else: move `VerifyDurableJournalReplay` and `VerifyPerGrainJournalIsolation` out of the factory into real tagged scenarios — `@Journals` for replay + isolation (keep their existing tolerance for cross-grain ordering: assert ownership/isolation only, never order across grains — Phase-1 flakiness is by design); the soft no-assert blocks inside the old replay routine (cross-LLM authoring, voice, agent) become real scenarios with real assertions tagged `@Distribution`/`@Voice`/`@Agent`, or are deleted — a check that cannot fail is deleted by definition.
- Tagging pass on ALL existing scenarios: `@Distribution` (additive — the FQN filter contract is untouched), `@GoogleAuth`, `@Rules`, `@Journals`, `@Voice`, `@Agent`, `@IAspire`, `@Marketplace`, `@Packager`; reserve `@Ui` and `@FreshCluster` for SIM3/perf.
- Coverage meta-test in `ProjectReviewTests`: every neuron interface in the sourcegen `KnownContracts` has ≥1 covering scenario (by tag or touched synapse types); failing message names the uncovered interface.
- Audit `InoTests.cs`/`ProjectReviewTests.cs` overlap with the tagged catalog; fold or tag, don't duplicate.
Gate: high-sev FQN filter 0 fail; `dotnet test --filter` by trait works for at least `@Journals` and `@GoogleAuth`; wall-time delta vs SIM0 baseline recorded in the plan; run-ci green.

**SIM2 — synapses + catalog + simulation.cs (headless).**
- Synapses per SIMULATION-PLAN §3.2: `RunSimulation(string Filter, SimulationMode Mode)`, `SimulationScenarioResult(Name, Source, Outcome, Diagnostic, RenderedSurface)`, `SimulationReport(RunId, Filter, SimulationScenarioResult[] Results, Passed, Failed, Skipped, ArtifactPath)` — concrete arrays, `[Id(n)]`, collector + probe round-trips in the bindings for every new type.
- `SimulationCatalog` (Core): index of compiled scenarios (feature/scenario/tag — from the generated Reqnroll classes' traits) + capsule scenarios (installed/packaged experience ids → their `scenario` blocks via the existing Ino parser). Filter grammar v0: bare token = OrdinalIgnoreCase-contains across tag, feature title, scenario title, class FQN, experience id; prefixes `tag:` `neuron:` `synapse:` `ino:` `scenario:` for precision (`neuron:`/`synapse:` resolve through `KnownContracts` to tags/ids — thin v1 seed, no auto-tagging engine).
- `simulation.cs` at repo root (file-based app, `#:project` the needed libs): parses `<filter> [--ui] [--list] [--ci]`; `--list` prints the catalog and exits; compiled leg spawns the MTP test exe with the mapped filter and parses the machine-readable report (the SIM0-verified switches); capsule leg runs filtered capsules through an in-proc sim world (the cleaned `Simulation` factory) using scenario blocks + `ReplayObservedSynapsesAsync`; merges into one `SimulationReport`; writes the SD4 JSON artifact; exit code = min(Failed, 1). It owns ZERO execution logic — thin by contract.
- `run-ci.ps1` delegates: environment hygiene (SKIP_FLUTTER_RESOURCE, pa-files pre-clean) + `dotnet build` + `dotnet run simulation.cs -- "Distribution" --ci`; the bespoke retry/summary parsing moves into simulation.cs (one owner of "what green means"); the direct FQN `dotnet test` command stays documented in README as the raw gate.
Gate: `dotnet run simulation.cs "Distribution"` reproduces the high-sev gate result (same scenario count, 0 fail); `simulation.cs "google"` and `simulation.cs "tag:Journals"` select correctly; run-ci green end-to-end through the delegation.

**SIM3 — UI watch mode (requires Flutter SDK + playwright install on this machine; if absent, land the harness + Skip-with-reason and record precisely — do not fake green).**
- Extract `SimulationUiHost` from NeuronE2ETest: AppHost boot via `DistributedApplicationTestingBuilder.CreateAsync<Projects.DigitalBrain_AppHost>` WITHOUT `SKIP_FLUTTER_RESOURCE` (flutter-client web-server target), Playwright lifecycle (headed locally / headless `CI=true`), endpoint resolution, screenshot-to-`pa-files/simulations/{runId}/`.
- Rewrite the E2E assertions as `@Ui` scenarios: surface renders in real Flutter web (alarm card), tap → ClientTap back-channel → brain receives — REAL asserts; prerequisites missing → `Skip` with the named reason (Flutter SDK absent / playwright not installed / endpoint dead). Delete NeuronE2ETest (SD2) → DELETED.md.
- `simulation.cs --ui`: run the filtered set against the booted topology, keep the world alive, open the browser per SD3, print dashboard + flutter URLs, screenshot each asserted surface, wait for Ctrl+C.
Gate: with SDKs present — headed run visually shows the alarm surface + tap round-trip with real assertions and produces screenshots; without — `@Ui` reported Skipped-with-reason in the merged report, never passed; high-sev 0 fail; run-ci green.

**SIM4 — the product doors.**
- `SimulationHostNeuron` (kernel experience; register like other experiences, capsule under `pa-files/marketplace` per the microkernel direction): `IHandle<RunSimulation>` → resolve filter against the capsule catalog (installed bundles + scenario blocks) → `StartQuarantineWorld` → install → execute scenario blocks + evidence replay → emit `SimulationReport` + a report surface (Card: per-scenario rows green/red + diagnostics, Buttons: Re-run, Open artifacts) → tear down unless Mode=Ui (then emit a surface carrying the sim world's connection info). Quarantine writes ONLY under `pa-files/simulations/{runId}` (stay out of the packages path — lock-contention landmine).
- Prove the L2 unification: the marketplace quarantine install replay and `RunSimulation` call the same execution path (one method, two callers) — add an assertion-level scenario for it.
- SD1 dev-mode: capability-gated MTP-exe spawn merged into the same report; grant flow journaled via the existing D3 Capability* synapses.
- Doors: Flutter Creator Run button = ValidateIno → pack → `RunSimulation(ino:<id>)` → report surface rendered (Dart `buildFromUiWidget` already handles Card/Button); TUI `/simulate <filter>` in ClientActions/CommandRouter; ino agent `run_simulation(filter)` tool in the AgentNeuron tool set; `run-simulation` typed command on the kernel Aspire resource next to `publish-experience`.
- New BDD (tagged `@Simulation`, separate feature file — high-sev stays the pure Core Law set): pack a rule capsule with a scenario block → `RunSimulation("ino:<id>")` via the brain → collector observes `SimulationReport` with Passed≥1 and the report surface whose `WidgetTree.Render` contains the scenario name.
Gate: the new `@Simulation` feature green; capsule pack→Run→report-surface loop proven via collector; high-sev FQN filter 0 fail; run-ci green; `dotnet run simulation.cs --list` shows the capsule scenarios.

## Landmines (do not relearn)
- The high-sev `FullyQualifiedName~DistributionDynamicHandlers` filter is load-bearing (run-ci, docs, muscle memory) — tags are additive; never rename/move those scenarios.
- New `[GenerateSerializer]` types: concrete arrays, `[Id(n)]`, collector + probe round-trips — every one, including `SimulationScenarioResult[]` inside `SimulationReport`.
- Per-grain journal ordering is Phase-1 flaky by design — `@Journals` scenarios assert ownership/isolation, never cross-grain order.
- `DistributedApplicationTestingBuilder` needs the AppHost PROJECT — ino.cs is not a test target; do not delete or thin the AppHost project.
- Playwright requires one-time `playwright install` — detect and Skip-with-reason, never tolerate-and-pass; the tolerant try/catch shape of the old NeuronE2ETest must not reappear anywhere.
- pa-files lock contention: pre-clean stays in run-ci; all simulation artifacts under `pa-files/simulations/{runId}`, never under `pa-files/packages`.
- Kernel-side process spawn (SD1) is privileged: D3 grant surface, journaled decision, no silent spawn.
- Awesome assembly must not reference `SurfaceStreamService`; seeding stays Aspire `AddStartupTask` single-owner Interlocked-guarded.

## Definition of done
SIM1, SIM2, SIM4 complete and committed (one commit each, deletions listed). SIM3 complete if Flutter SDK + Playwright are present on this machine; otherwise the harness + Skip-with-reason lands and the precise environment gap is recorded. `git status` clean except commits; run-ci green at end (through the simulation.cs delegation); high-sev FQN gate 0 fail; DELETED.md updated (NeuronE2ETest, factory suites, soft no-assert blocks, run-ci parsing); SIMULATION-PLAN.md §6 stage table marked with commit hashes + the SIM1 wall-time delta; README gains the simulation.cs quickstart (`--list`, filter examples, `--ui`); USER-FLOWS.md gains the Creator-Run-simulation flow. Code review pass before return. Update this document with a completion marker (commit hashes + open follow-ups).
