# Continuation prompt — OS-FROM-INO full run (OS0 → OS7)

You are a senior C# developer and Microsoft Orleans expert working on **DigitalBrain** at `E:\Projects\final` (.NET 11 preview 4, Orleans 10.1.1-preview.1 + Journaling alpha, Aspire 13.4.x/13.5-preview CLI, Hex1b TUI 0.164.1, Reqnroll BDD, xunit v3 + Microsoft.Testing.Platform, Flutter client). Use context7, microsoft-learn, and the hex1b skill/MCP for every API before writing code. Tooling constraints: `Filesystem:search_files`/`directory_tree` crash on this repo — use `list_directory` + targeted `read_text_file` only; never use `C:\Users` paths in any command; all paths relative inside `final/`. Code rules: no comments (self-documenting only), production-ready, no `GrainFactory` in constructors, `[GenerateSerializer]` + sequential `[Id(n)]` on every serialized type, concrete arrays only, NEVER collection expressions assigned to `IReadOnlyList<T>` on serialized types. Latest packages stay pinned via `Directory.Packages.props` — do not downgrade.

## Mission

Execute **the entire `docs/OS-FROM-INO-PLAN.md` (stages OS0 → OS7) in this single session**, testing constantly. That plan is the authoritative Step-1/2 contract — READ IT FULLY FIRST, plus `docs/UNIFICATION-PLAN.md`, `docs/INOLANG-RFC.md` (the D2 freeze is binding), `docs/PRODUCT-SPEC.md` §2, and `docs/SIMULATION-PLAN.md`. Process is The 5 Steps in order; Steps 1–2 are already done in the plan — do not re-litigate them, but DO flag (in the handoff note) anything you discover that invalidates a Step-1 verdict.

**Owner authorization (Vlad, 2026-06-12, via this prompt):** the recorded defaults **D-OS1 through D-OS7 are ACCEPTED as written**. OS0 records them as answered-by-default in the plan's decision section. No further owner input is required to start; questions that arise mid-run get the dumbest honest resolution + a line in the handoff note, never a silent assumption.

## One run, eight commits

"One run" means all stages this session — it does NOT mean one commit. The ritual is unchanged: **one commit per stage**, message lists deletions, `docs/DELETED.md` + `ROADMAP.md` + `USER-FLOWS.md` + `DISTRIBUTION.md` + the plan itself updated at the stage that touches them. Tree clean except the stage's commit before moving on.

## Constant-testing protocol (non-negotiable, every stage)

1. **FIRST COMMANDS:** `git status` (clean tree), then `.\run-ci.ps1` (green baseline). Never start from an unverified baseline. HEAD should be `2243410` or a descendant.
2. **After every logical delta** (not every file save — every compiling unit of behavior): `dotnet run simulation.cs -- "Distribution" --ci` → must be 0 failures on the core DistributionDynamicHandlers scenarios before the next delta.
3. **Serialization first:** every new `[GenerateSerializer]` type from Appendix B gets its collector+probe roundtrip in `DistributionSimulationBindings.cs` and a green run BEFORE any behavior code uses it.
4. **After hosting/AppHost changes:** `aspire do build` (or `cd src/DigitalBrain.AppHost; dotnet build`) + one `aspire run` smoke where the stage gate demands it.
5. **Stage land:** full `.\run-ci.ps1` green → commit → next stage. A failing stage gate is fixed inside the stage; if it cannot be fixed, STOP the run, revert to the last green commit, append the handoff note — never proceed past a red gate and never weaken an existing assertion to get green.
6. **New scenarios extend, never fork:** N−1, grant-gated install, requires-check go into `DistributionDynamicHandlers.feature`; shell scenarios into a new `Workspace.feature` tagged `@Shell` (inner loop: `dotnet run simulation.cs -- "tag:Shell"`); gmail loop: `dotnet run simulation.cs -- "ino:gmail-last-senders"`.

## Stage checklist (gates condensed — full detail in the plan §6; follow the plan where they differ)

- **OS0:** baseline rituals; fill the decision row (defaults accepted); audit U4 leftovers — read `GoogleAuthNeuron.cs`, the AuthGoogle BDD, the Google stub seams, and the exact AuthLinkReady/OpenUrl surface shape U4 landed (read, don't assume). Doc-only commit.
- **OS1:** `brain.ino` (grammar + lowering table in plan §3.1) + BOOT001–BOOT010 validator (unit-tested, fatal at boot, line+code+message readable in a terminal) + `BootManifestApplied` journaled + `world: from` second manifest; `ino.cs` shrinks to parse→lower→run; DELETE the topology data from `AddDefaultDigitalBrainTopology` (C# remains as lowering target only); re-challenge the dashboard-token block. **Gate:** resource list from manifest boot compared equal to a pre-OS1 captured run; high-sev 0f.
- **OS2:** `SurfacePlacement` + append-only `UiSurface.Placement` (+roundtrips FIRST, old seeded `google-auth.brain` as the deserialization fixture) + `ShellNeuron` absorbing `UiNeuron` (delete it; migrate Username/AvailableBrains per D-OS7) + Pin/Unpin/Move/`WorkspaceChanged` + capsule-default placement application + TUI Home-tab probe (Grid: widgets column of titled Borders via unchanged `SurfaceRenderer`, main area, dock List) + `@Shell` headless input-sequence/screen-assert tests. **Gate:** pin → kernel restart → still pinned (root world); the four legacy TUI scenarios untouched; high-sev 0f.
- **OS3:** `os/` folder complete per plan §3.2 (all capsules, new header lines `region:/pinned:/order:/requires:/system:` through parser → manifest → packager — header lines ONLY, the D2 freeze forbids new rule statements) + `seed:` lines feed the single `AddStartupTask` owner (delete duplicate seeding + shrink hardcoded activation lists to the substrate set) + `UninstallBundle` (deactivate, RuleSet removal, contract-contribution removal, placement removal, `BundleUninstalled`; journal untouched; `system: true` refuses with a surface) + N−1 scenario + Installed-apps section on the marketplace surface + `requires:` check-and-surface (no solver). **Gate:** N+1 AND N−1 green; audit script proves activated experiences ⊆ seeded capsule ids ∪ substrate list; high-sev 0f. If the N−1 arithmetic cannot be made the exact inverse, implement the minimal structural-observation form of `ListSubscribersAsync` (ROADMAP Phase 1.5) rather than fudging counts — and record it.
- **OS4:** live persona builder (composed per-request from grain reads: installed bundles+versions+levels, WorkspaceState, tasks/alarms, peers/global summary, tool list; delete redundant hardcoded narrative) + OS tools (`list_installed_experiences`, `install/update/uninstall_experience`, `run_experience`, `pin_widget`, `move_widget`, `describe_workspace`) with emits-as-tools where the body is construct+Emit, destructive set behind the existing ApproveAction guard. **Gate:** persona-changes-on-install unit; scripted orientation exchange green against the stub LLM seam; high-sev 0f.
- **OS5:** pure-renderer shell — TUI regions-as-chrome (Splitter/DragBarPanel), Flutter workspace scaffold consuming `WorkspaceChanged` + `UiSurface`; DELETE the D5 prefix routing as primary (null placement now falls back to `main`) and any Flutter routing remnants. **Gate:** every USER-FLOWS flow re-verified through the workspace; `@Shell` headless green; `@Ui` Playwright screenshots (graceful skip-with-reason if Flutter/playwright absent — record the env gap, do not fake); high-sev 0f.
- **OS6:** `GmailNeuron` (Kernel/Experiences; `GmailLastSendersRequest(Count)` + ino-routed `AgentRequest`; token from the encrypted secret grain; HTTP/credential seam from DI so the existing tolerant Google stubs inject; emits `GmailSendersResult`, widgets-placed card, grant-gated `SaveFileRequest`) + `os/gmail-last-senders.ino` exactly per plan Appendix A + grant flow (`GrantRequested/GrantDecision/GrantRevoked`, stored per-bundle+capability in brain state, enforcement at install-time declared-emits and at RuleHost emission; record the amended Q2 in INOLANG-RFC) + marketplace **listing** (not preinstalled) + stub-driven BDD per Appendix C. **Gate:** the full install→requires→auth(stub)→grant→N+1→ask-ino→card→save scenario green; the real-Google OAuth demo is a MANUAL gate — write the 5-minute Google-console setup into the commit/docs and mark it deferred-manual; do not block the run on it.
- **OS7:** docs sweep (ROADMAP "Phase 6 — OS" → this plan; USER-FLOWS workspace + gmail flows; DELETED entries; DISTRIBUTION header-line deltas; INOLANG-RFC grant amendment; README flagship demo #3 script) + measure the §8 north stars you can measure headlessly (boot-to-workspace time, traceability audit, N±1, arrangement persistence) and record numbers in the plan. **Gate:** full `run-ci.ps1` green; clean tree.

## Landmines (full list in plan §7 — these kill runs)

Append-only `[Id]` on `UiSurface`; Region is a string, unknown → `main` + telemetry; exactly four regions, a fifth needs an owner decision; wildcard subscribers are exactly two (RuleHost, Shell) — a third stops the run; no secrets in any `.ino`; no assertions assuming per-grain journal durability (E3 is not done); `show card(...)` is NOT extended with placement; `bundle: ... as silo` stays BOOT010-reserved (no L3 this run); single-file AppHost is CLI-only.

## Definition of done

Eight commits OS0–OS7 (or a clean partial: complete stages only, last commit green, no half-stage on master); high-sev DistributionDynamicHandlers 0 failures at every land and at final HEAD; `run-ci.ps1` green at final HEAD; every Appendix-B type roundtrip-proven; the traceability audit passes; north-star numbers recorded in plan §8; handoff note appended to `docs/OS-FROM-INO-PLAN.md` (what landed per stage with commit hashes, env gaps, deferred-manual items, anything that should re-open a Step-1 verdict). Code review of the full diff before declaring done.
