<Project Sdk="Microsoft.NET.Sdk.Web">
  <PropertyGroup>
    <TargetFramework>net11.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <LangVersion>preview</LangVersion>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="ModelContextProtocol.AspNetCore" />
    <!-- Force MessagePack central latest (3.1.7) over any old transitive for vuln hygiene. -->
    <PackageReference Include="MessagePack" />
  </ItemGroup>

  <ItemGroup>
    <!-- Reuses AddServiceDefaults / MapDefaultEndpoints + OTEL/resilience wiring (same as Kernel). -->
    <ProjectReference Include="..\DigitalBrain.Aspire.Hosting\DigitalBrain.Aspire.Hosting.csproj" />
    <!-- DigitalBrainCluster + client config + IDigitalBrain/IMarketplace/IAspire grain access for the MCP tools. -->
    <ProjectReference Include="..\DigitalBrain.Hosting\DigitalBrain.Hosting.csproj" />
  </ItemGroup>
</Project>
