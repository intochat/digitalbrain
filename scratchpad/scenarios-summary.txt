S01|WORKS|stage=8|Sales dashboard, live, shared|dataOnly=True|components=[ingress,chat,graph,mcp,cell,schedule,view,identity]|residual=Ticks due while the silo is down are permanently missed; recovery is ONE collapsed tick marked Resolution=Recovered, so 'every 15 minutes' holds while the silo is up. Sharing is one grant per subject per person on the OWNER's data — no grant-a-bundle verb. Chart retention is per-series capped, so history past the cap lives only in the corpus.
S02|WORKS|stage=8|One board, three source cells|dataOnly=True|components=[tools,cell,bundle,view,ingress,outcome]|residual=A board is an ordered list of REFERENCES with PartKind/Title — no persisted free-form geometry, because drag-resize is continuous interaction and permanently out. The countdown is client-side interpolation. A restart drops all three streams; the resume loop re-attaches, but a cursor outside the 512-entry ring forces a full re-GET, so the viewer sees a snap rather than a scroll.
S03|WORKS_PARTIAL|stage=7|Two upstreams, one revenue chart|dataOnly=True|components=[mcp,graph,kind,cell,outcome]|residual=The merged chart lands; the weekly AXIS does not. The two series key their weeks by different conventions and the residual's own fallback concedes an ISO-week derivation would be a palette primitive — a compiled release, contradicting dataOnly. Also standing: the per-owner graph grain is non-reentrant, so a 200-row fan-out from both servers serializes behind memoized graph calls with no backpressure beyond quotas.
S04|WORKS|stage=7|A live map of myself|dataOnly=True|components=[ingress,tools,graph,cell,substrate,corpus,view,identity]|residual=The highlight is a PROJECTION: the drain is asynchronous and a corpus-write failure never fails a turn, so 'live' means 'live within the drain'. Connection ADDITIONS are not routable events — only removals are — so the map's growth is poll-shaped. And neither SynapseConnection nor a corpus line carries a PrincipalId, so 'principal-scoped topology' and a principal-filtered pulse need a field the amended spec does not define; inside a multi-principal owner the map is the owner's partition, not the person's.
S05|WORKS_PARTIAL|stage=6|One-tap approval button in chat|dataOnly=True|components=[schedule,substrate,mcp,cell,chat,ingress,graph,outcome]|residual=The refusal half — the scenario's stated hard part — is fully carried. The trigger half is reduced: there is no 'sync finished' fact, so the offer hangs off ui.chart-changed (rows arrived), which is silent on an empty night and early on a partial one. The HTTP 202 is an acknowledgement; the reason arrives as a later frame. And if the nightly sync is real I/O rather than a scheduled MCP pull, its completion fact comes from the Stage-10 behavior host, pushing this to Stage 10.
S06|WORKS|stage=4|Focus countdown inside the conversation|dataOnly=True|components=[ingress,chat,model,graph,cell,view,substrate,outcome]|residual=Stage moved 1→4 under challenge: Stage 1 closes both named killers (correlation inheritance, unrouted visibility) but the note morph needs the quoted literal and required-field coverage that land in Stage 4. A restart spanning the deadline misses the tick outright; recovery is late and flagged Recovered. The visible countdown runs on the client's clock — true wall-clock at the receiver is permanently out.
S07|WORKS_PARTIAL|stage=2|One chart, one teammate, nothing else|dataOnly=True|components=[ingress,tools,identity,cell,outcome,corpus]|residual=The chart-sharing rail is real and enforced per read. The isolation ABSOLUTE is not: four owner-scoped stores have no principal filter in the amended spec, and §F protects MCP payloads under an OWNER purpose. Making this WORKS needs principal-scoping (or grant-checking) corpus, inbox, registry and topology reads. The operator boundary is separately and permanently out: cluster membership IS the authority.
S08|WORKS|stage=6|Revoke a teammate mid-stream|dataOnly=True|components=[ingress,tools,identity,cell,outcome,corpus,chat]|residual=Eviction is push-side only: frames already flushed are hers, and anything rendered or screenshotted is beyond recall. Two flags carried from the challenges: nothing in §K or §M constrains WHO may fire grant.create, so the cut is an act performed rather than a property maintained; and because corpus/inbox stay owner-scoped (see S07), cutting the chart grant does not by itself cut a co-principal off from chart lines in the corpus.
S09|WORKS|stage=8|The board survives the redeploy|dataOnly=True|components=[substrate,schedule,identity,graph,mcp,cell,ingress,view,outcome]|residual=Ticks due while the silo is down are permanently missed; recovery is ONE collapsed tick, so an eight-hour redeploy yields a current board, not eight hours of points — 'still refreshing' is true, 'never stopped' is not. Per-render latency over Azure Blob is unmeasured, and instances are pinned to their kind version, so a palette release retiring a view node kind leaves a pinned board document unrenderable with no migration underneath.
S10|WORKS_PARTIAL|stage=8|Pop the chart out|dataOnly=False|components=[chat,tools,cell,view,ingress]|residual=The OS window is the ask, and the programme's own honest-limits list calls it not reachable. The user tiles the whole shell instead; adding a window verb is a Flutter client release, which is why dataOnly is false. Everything else is met exactly, and because the panel binds to a subject rather than a widget instance, no kind, connection or record would change if the shell ever gained multi-window support.
S11|WORKS|stage=5|SOQL aliases become chart points|dataOnly=True|components=[chat,tools,graph,mcp,cell,outcome,view]|residual=Value is a double, so SUM(Amount) is presentation-accurate, not accounting-accurate. Batch validation is all-or-nothing: one currency-formatted column blocks the chart until the model re-authors the SOQL — it now gets row+field and a fix path, but nothing coerces. FireRowsAs stays row-capped with Truncated reported. The chart enters the CONVERSATION only at Stage 8 via Responded.Views.
S12|WORKS_PARTIAL|stage=7|Gmail arrival becomes a note|dataOnly=True|components=[kind,cell,ingress,mcp,chat,schedule]|residual=The mechanism is real and 'once per email' is genuinely carried while armed. The quantifier 'every time' is not: renewal is data the owner must build, and its failure mode is silence — exactly the class this programme claims to have eliminated. Push is also at-least-once against a 512-entry Seen map, so a burst past that window can re-note. The signature verifier is compiled palette and the Pub/Sub topic is created in Google's console.
S13|WORKS_PARTIAL|stage=5|First Salesforce sign-in mid-conversation|dataOnly=True|components=[chat,model,mcp,outcome,ingress]|residual=The OAuth mechanics are fully repaired. What is unresolved is the seam two sibling scenarios also name: a human browser trip is minutes long and the chat policy that cancels a parked head is 15 seconds. Until that is reconciled the resume works only if the owner signs in fast, or the turn re-enters as a fresh journaled beat on the same root. Pending-state caps become per (serverKey, principal), so a principal can still exhaust their own slots.
S14|WORKS|stage=6|Six a.m. pull, nobody awake|dataOnly=True|components=[schedule,identity,graph,substrate,mcp,cell,outcome]|residual=A tick due while the cluster is down is permanently missed; catch-up collapses to ONE phase-preserving Recovered tick, so '6am' degrades to 'first thing after the silo is back' — acceptable here because the deliverable is a CURRENT chart, not a timestamped artifact. The call spends the owner's stored OAuth token with no human present: deliberate, recorded as OnBehalfOf, stopped by revocation, but it is offline credential use and should read that way on the consent screen. Chart values stay doubles.
S15|WORKS|stage=6|Overnight token expiry, silent refresh|dataOnly=True|components=[outcome,tools,corpus,model,mcp,cell]|residual=invalid_grant — revoked, policy-expired or password-invalidated refresh token — cannot be repaired by any architecture; the system degrades honestly to a card, and 'without making me log in again' is then unmeetable. Recovery is proactive (skew), not reactive: an unexpected mid-window 401 surfaces as a refusal rather than a silent retry-after-refresh. And a ROTATED refresh token must be re-persisted on every exchange — the amendment does not say so, and providers that rotate will work exactly once if that is missed.
S16|WORKS_PARTIAL|stage=5|Two teammates, two Salesforce orgs|dataOnly=True|components=[tools,identity,ingress,chat,graph,mcp,outcome]|residual=OAuth separation is genuinely earned. The partition around it is not: five owner-scoped stores are shared, both principals serialize behind one graph grain and share one connections quota, and 'her own chart, not mine' rests on undiscoverable naming rather than a stated write rule. Fixing this is a stated cross-principal connect refusal plus principal-scoping the inbox/corpus/registry.
S17|WORKS|stage=5|Provider ships a new tool|dataOnly=True|components=[chat,tools,mcp,model,substrate,outcome]|residual=Upgraded from PARTIAL under challenge: both prior reservations are sizing notes, not failures of this ask. McpToolsListed stays unpaged, which is survivable at Salesforce size and fatal on a very large catalog; and find_capabilities indexes compiled contracts, kinds and library artifacts but never live MCP catalogs, so free-text discovery of a provider tool by name still scores zero — irrelevant here because the owner's own sentence names the server and get_neurons resolves the instance.
S18|WORKS|stage=5|Deleting a real Salesforce record|dataOnly=True|components=[chat,mcp,substrate,outcome,ingress,corpus]|residual=Exactly-once against an external provider is unattainable and the architecture says so: a delete whose response is lost settles OutcomeUncertain and a human verifies. Two edges: DestructiveHint defaults TRUE when unannotated, so a provider that annotates nothing puts a press in front of every call; and §N bounds only the FireRowsAs reply, never a plain call-tool's Content, so readability depends on a narrow SOQL. §N also says one-shot OWNER approval while the gate binds a PRINCIPAL — in a multi-principal owner, who approves whose credential spend is unstated.
S19|WORKS_PARTIAL|stage=7|Behavior that pulls from Salesforce|dataOnly=True|components=[tools,schedule,cell,graph,model,bundle]|residual=The identity and credential story is fully carried: the tick acts as a person, no run token freezes an author's credential, and no SDK is needed on the silo. Two mechanism gaps keep it partial — the model's answer reaches the graph only through a cell's ask+resume step (not by routing AiAnswered), and the weekly fold needs a completion signal and a reset that the seed sum kind plus wire do not supply. If the owner insists on the literal compiled-behavior form, that is Stage 10 with an integration-authorization reference rather than a raw principal.
S20|WORKS|stage=5|Two hundred rows in one turn|dataOnly=True|components=[chat,graph,tools,mcp,cell,model,corpus]|residual=The 200-row cap is a cap, not a pager: Truncated/RowsAvailable tells the truth about row 201 but there is no continuation cursor, so 'all 213' needs a narrower query. Journals stay 512-entry rings, so between Stage 5 and Stage 6 the audit is ring-bound. The per-owner graph grain is still the scaling wall and the only backpressure is quotas.
S21|WORKS|stage=8|Salesforce rows fill a live chart|dataOnly=True|components=[chat,tools,graph,mcp,cell,view,outcome]|residual=Ordering is causal, not enforced: a second actor triggering mcp concurrently can still emit ahead of a wire — now surfacing as db.unrouted rather than vanishing. 200 rows are 200 serialized deliveries each with a WriteStateAsync over Blob; memoization removes graph round trips, not commits, so a large set fills visibly slowly with no backpressure. Per-series retention trims past its cap with Truncated as the only signal. An unauthorized Salesforce parks the turn on a sign-in card — the same 15-second policy seam as S13.
S22|WORKS|stage=4|Model guesses timer:dev/timer|dataOnly=True|components=[chat,tools,graph,outcome,schedule]|residual=The guess is not prevented, only made cheap and loud: one hop is burned on the wrong name before the refusal teaches the right one. Punctuality belongs to the substrate — a 20-minute elapse whose reminder is due during a restart fires after the outage, so 'when it goes off' means within the restart window.
S23|WORKS|stage=6|Why is this chart wired that way|dataOnly=True|components=[chat,tools,graph,corpus,schedule,model]|residual=Wires created before the provenance amendment render as 'provenance: unrecorded' with no backfill. StatedIntent is the model's own sentence at connect time — the Actor half is unforgeable, the PURPOSE half is exactly as good as what was typed. Corpus blobs roll but are never compacted and carry no retention policy, so an explain for a very old wire returns the record and its provenance without the originating episode.
S24|WORKS|stage=4|Rewire the chart to plot amount|dataOnly=True|components=[tools,graph,outcome,mcp,view,corpus]|residual=Widening-only bites here: Salesforce Amount is decimal and ChartPoint.Value is double, so the mapping is legal only because a lossy numeric is accepted — exact decimal money is a named permanent exclusion, and a revenue chart is where that shows. 'Don't leave the old one running' is fully carried for the WIRE; the stale points clear only because the reset is fired, and nothing infers that from the sentence. At Stage 4 the rewire is correct but watchable only once Stage 8's bound view exists.
S25|WORKS_PARTIAL|stage=8|Laptop closes mid-turn|dataOnly=True|components=[ingress,chat,mcp,substrate,outcome,cell,view]|residual=The scenario's stated failure — returning to a turn marked cancelled you never cancelled — is fixed only if reconciliation stops applying CancelExecution, and no amendment says so. The 15-second WaitingPolicyDeadline that cancels a parked head is likewise unaddressed and collides with Stage 5's browser-trip blocker. Separately: abandonment re-attempts from the top, so an hour of streamed prose is discarded and the inference is paid twice.
S26|WORKS_PARTIAL|stage=10|Two chats, one assistant grain|dataOnly=True|components=[ingress,chat,substrate,graph,model,tools,identity]|residual=Principal-scoping fixes only the cross-principal half. The same principal's two windows share one assistant grain by default and nothing in the amended spec binds a second responder automatically, so the remedy is data the owner must apply. Below that is a physical floor the architecture declines to move: one Gemma4 12B on one GPU means time-sliced, never parallel. Both windows also still serialize on the per-owner graph grain and share one session journal ring.
S27|WORKS_PARTIAL|stage=6|Cancel a turn already half-executed|dataOnly=True|components=[ingress,chat,substrate,outcome,corpus,graph,mcp]|residual=Two real reductions. Typing 'Stop — cancel that' into a busy conversation preempts nothing: chat.send-message enqueues behind the running turn and keyword god-switches are banned, so the preemptive path is the stop control or a directly fired chat.cancel-turn. And cancel is not undo — the architecture converts undo into disclose-plus-compensate. The Cancelled-vs-Succeeded report also rests entirely on Execution's OutcomeUncertain reconciliation being right.
S28|WORKS_PARTIAL|stage=7|Refuse a daily email honestly|dataOnly=True|components=[tools,mcp,identity,bundle,schedule,graph,cell,outcome]|residual=Two independent reductions. Content: the day boundary is the provider's, not the schedule's, so 'yesterday's closed-won total' can be the wrong day's number and nothing in the data tier reconciles them. Delivery: one silo means a tick due during an outage is permanently missed and collapses to ONE late Recovered tick — for a timestamped email that is a wrong artifact, not merely a late one. The body sentence is model-written prose around a bound number: the number is auditable, the sentence is not.
S29|WORKS_PARTIAL|stage=7|Compose a pipeline watcher from discovery|dataOnly=True|components=[tools,bundle,schedule,graph,mcp,cell,outcome]|residual=The wiring, the install atomicity and the alert routing all land; the arithmetic input does not. Without a batch-complete signal and a per-tick reset the sum double-counts, so the threshold fires off a number that is not the pipeline total. Beyond that: currency equals the cadence, so a move that reverses inside one interval is invisible; a pipeline wider than the receivers cap is truncated (disclosed); and a six-member install document authored in one 4096-token turn is realistically two or three refuse-and-fix rounds.
S30|WORKS|stage=9|Teach me what this brain does|dataOnly=True|components=[tools,kind,graph,schedule,behavior,bundle,library,corpus,identity]|residual=Truthful but necessarily paged: catalog mode pages by construction, corpus reads return receipts, and one 4096-token reply cannot render a month-old brain in one breath — the owner gets a guided walk with drill-down handles, and completeness depends on the model paging rather than stopping after the first section. Two honesty flags: 'act by calling a tool immediately' is answered by reading rather than by a repealed instruction, and grant.list does NOT cover co-principals of the same owner, who reach the owner-scoped corpus, inbox, registry and graph with no grant at all.
S31|WORKS|stage=7|Nightly hours check from one sentence|dataOnly=True|components=[chat,tools,kind,cell,schedule,graph]|residual=Where the hours come from is the owner's problem: logged in chat this is pure data; from a tracker it needs the MCP rail and a second schedule. The day boundary is the tick, not midnight — an outage across 21:00 fires ONE Recovered tick at restart and hours logged in the gap land in the wrong bucket. And it all rests on the model emitting valid canonical kind JSON: the refusal is path-precise and same-turn, but the evaluator ships with no automated tests by owner amendment.
S32|WORKS|stage=6|Scheduled Salesforce pull fans into chart|dataOnly=True|components=[mcp,schedule,graph,substrate,identity,cell,outcome]|residual=Connect-time validation cannot see Salesforce's schema: a renamed column is caught at delivery, where all-rows-before-first-emit converts it into a settled refusal — visible, but one tick late. Ticks due while the silo is down are permanently missed and collapse to one Recovered tick, harmless here because a batch pull is idempotent. The first authorization is irreducibly human, and after a revocation every tick refuses forever — the inbox says so, but nothing disarms the schedule.
S33|WORKS|stage=10|Behavior wires its own route|dataOnly=False|components=[chat,behavior,substrate,cell,graph,tools]|residual=The trail is the Provenance record plus the corpus line, NOT a reification fact — ISynapseGraph stays on FrameworkInterfaces, so 'who edited my graph' is answered by a different record than the review expected, equivalent only because provenance became mandatory. A behavior's allow-list is over ALIASES, not targets, and the send-within-membership restriction covers kinds, not compiled behaviors, so a run holding db.connect can rewrite any of that owner's topology. Authoring is still C#, and a diagnostic set is a coarser correction signal than the kind tier's path-precise refusal.
S34|WORKS|stage=10|Chart update that fires nothing|dataOnly=False|components=[cell,kind,graph,schedule,mcp,behavior,view]|residual='Whenever' is never both instantaneous and complete: MinInterval alone is leading-edge, so the trailing form costs the extra schedule and quantizes latency to its cadence. The 200 change emissions still cost 200 chart turns and 200 graph lookups — cheap next to 200 process launches, not free, and memoization does not span turns. Sourcing the trigger from the MCP batch summary would be exactly-once and post-complete, but then a manually added point stops re-running the forecast.
S35|WORKS|stage=10|Broken behavior source, contained crash|dataOnly=False|components=[outcome,behavior,substrate,tools,corpus,graph]|residual=Partiality is made LEGIBLE, not fixed: a run's fires are extra-turn commits and nothing rolls them back, so 'failed' can still mean 'half of it happened' — the contribution is being able to enumerate which half against a record that existed before the launch. The corpus is a background projection, so a crash during the drain leaves the explanation lagging until the watermark catches up. Stderr is truncated at the sender and a library failure surfaces as an exit code rather than a typed fault.
S36|WORKS|stage=10|Behavior hangs past its bound|dataOnly=False|components=[tools,behavior,substrate,outcome,corpus,chat]|residual=Kill-by-Pid has no process-start-time guard anywhere in the programme, so after a host reboot a reused Pid could be killed; a grandchild that double-forks escapes the tree kill. 'What it was doing' is bounded by what crossed the rail — internal state at kill time is unobservable. Fires the run already committed cannot be rolled back by the kill.
S37|WORKS_PARTIAL|stage=10|Behavior fires off its allow-list|dataOnly=False|components=[tools,behavior,outcome,identity,corpus,graph,chat]|residual=The allow-list is an authority boundary over the brain's rail, not an egress boundary over an OS process. A behavior is arbitrary C# — possibly model-authored through behavior.create — running as a child of the silo with a working network stack; a hardcoded webhook URL in its own source reaches the team channel without touching DigitalBrain, and the brain can neither prevent nor observe it. The honest answer then degrades to 'not through my rail; here is the source at SourceVersion v3 and the zero fires it made'. No container, no seccomp, no per-run OS identity anywhere in the programme.
S38|WORKS|stage=10|Edit the source, read the history|dataOnly=False|components=[tools,behavior,substrate,corpus,chat]|residual=Bulk history is a receipt and a link, not a table in chat. The corpus is append-only and explicitly never compacted, so 'every run' grows without bound and a retention policy is owed. A lease-abandoned run appears as attempt-failed(WorkerAbandoned) rather than with the output it intended. One flag from review: OnBehalfOf is captured at CREATE and nothing re-verifies it when set-source replaces the executable — the edit changes what code runs while leaving whose tokens it spends unchanged, and no stated rule restricts who may fire set-source.
S39|WORKS|stage=10|Behavior reaches another person's chat|dataOnly=False|components=[tools,behavior,schedule,graph,identity,chat,outcome,model]|residual='All my conversations' means conversations this principal OWNS — a chat held only by grant is outside the index and silently absent. The summary has no acceptance criterion inside the system. One GPU plus the inference budget makes a many-conversation prompt slow or partly refused, and the prompt bound is unspecified. A missed 07:00 reappears as ONE Recovered tick. Flag: the allow-list is alias-scoped, so db.read with this principal also reaches the owner-wide corpus and inbox, which 'my conversations' does not bound.
S40|WORKS_PARTIAL|stage=10|Owner script versus owner behavior|dataOnly=False|components=[ingress,identity,graph,corpus,behavior,outcome,substrate,chat]|residual='Never' holds for every path the brain exposes and fails outside it. The run is a child of the silo, on the same host, under the same OS identity, with filesystem and network access and no sandbox anywhere in the programme — so the clustering string and the token signing key live in files and environment the run can read. The boundary is a credential check and the credential material is inside the blast radius. And behavior source can be model-authored, so the guarantee is a policy over what the brain hands the child, not a property of the child.
S41|WORKS|stage=6|Bundle a built dashboard into an app|dataOnly=True|components=[tools,graph,schedule,bundle,outcome,corpus]|residual=One user-visible turn, two durable grain commits: the bundle record and the denormalized bundle-id/Enabled pair on the graph's connections are separate writes, so a crash between them leaves the app DISABLED with one unstamped member — visible in describe and re-runnable, but not one atomic commit. Intent is verbatim only where a StatedIntent was supplied at connect; earlier wires print provenance-absent and are explained only indirectly.
S42|WORKS|stage=4|Disable the app, everything stops|dataOnly=True|components=[ingress,tools,bundle,graph,substrate,outcome,schedule,mcp]|residual=Correctness holds; the cost claim was overstated and is corrected above — the drain's re-check is a real per-entry graph call into the single non-reentrant grain, which is exactly the second hop the scenario calls prohibitive. 'Right now' is bounded by one drain interval: a delivery already accepted finishes its turn, and an MCP request already issued still executes at the provider. A behavior member's ActiveRun keeps running to its deadline unless separately cancelled — disable stops triggers, not children.
S43|WORKS_PARTIAL|stage=9|Export to a teammate's own Salesforce|dataOnly=True|components=[tools,library,bundle,mcp]|residual=There is no directed hand-off verb — publish/approve/deprecate/find/describe/instantiate/upgrade plus a deprecation fan-out is the whole surface — so 'send it to Dana' is publish, her discovery and her deliberate install (auto-install was killed on purpose). Cross-deployment it degrades further to bundle.export plus verbatim describe plus human approval: the hash proves the bytes, never the author, because there is no asymmetric signing. Installs copy, so a later fix never reaches her. One invariant gap: a member connection whose Source is authored as a literal mcp:dev/{vlad:N}.salesforce is neither GUID-shaped nor integration/-prefixed, and inside the same owner it would resolve to Vlad's gateway.
S44|WORKS|stage=6|Why did you email that quote?|dataOnly=True|components=[chat,tools,corpus,schedule,graph,model,mcp]|residual=The answer covers only the window the projection has been running — journals are 512-entry rings and anything predating the corpus is unrecoverable, so this is a going-forward guarantee. Depth beyond the spine is a receipt plus URI, rendered rather than read inline. A corpus-write failure never fails a turn, so a lagging drain leaves a gap a query run now will not see. Flag: §F protects MCP bodies under an OWNER purpose and a corpus line carries no PrincipalId, so 'unsealed only for the asking principal' is a policy the amended spec does not supply.
S45|WORKS_PARTIAL|stage=6|That chart is wrong, mark it|dataOnly=True|components=[chat,tools,corpus,outcome,graph,cell]|residual=Nothing puts the mark in front of the model at the moment it would repeat the mistake: feedback.given is a corpus fact, the vector Memory module is deleted by design, and episode intents feed only library discovery. 'So you don't do that again' holds for THIS chart, through the tombstoned wire and the corrected literal, but not as a standing constraint on the next one. Making it standing means editing compiled assistant instructions — a palette release with cluster-wide blast radius.
S46|WORKS_PARTIAL|stage=6|Six months of episodes as training data|dataOnly=True|components=[chat,tools,corpus,identity]|residual=Four reductions. 'Where I told you that you got it wrong' is labelled only where feedback.given exists — a correction typed as prose stays unlabelled. The window exists only forward of Stage 6. Redaction closed as retain-but-protect with no scrub-on-export, so a training-data export writes OAuth-scoped Salesforce and Gmail bodies in the clear — and under an OWNER purpose, so co-principals' bodies too. The six-month fold is unbounded compute the receipt-plus-URI contract implies but the spec never names the job for.
S47|WORKS_PARTIAL|stage=10|Silo rebooted mid-rollup|dataOnly=False|components=[behavior,substrate,outcome,tools,corpus,chat]|residual='Did it finish' degrades to OutcomeUncertain in exactly the window the crash covers: a crash between subprocess exit and record-commit can lose the run record, and already-committed fires cannot be rolled back, so 'finished' is answered per step, never atomically. The verdict also lands up to one liveness period (~60s) after restart. dataOnly is false for the traced form; a rollup expressible as schedule + call-tool + seed kinds would be pure data and would answer this better, because every hop would be a journaled turn with no process memory.
S48|WORKS|stage=6|Hourly sync slept six hours|dataOnly=True|components=[schedule,substrate,identity,graph,mcp,corpus,chat,outcome]|residual=The ask — six times or once? — is answered exactly and as a recorded fact. Two honest edges: recovery latency is bounded by when the reminder next fires after the cluster returns, which is why the fact carries both DueAt and ObservedAt; and recovering the six hours of DATA (rather than the tick) needs a cursor-bearing cell, since the wire language cannot splice a value into a SOQL string. A laptop that sleeps most of the day gets one tick per waking window — the honest semantics of a schedule on a single silo.
S49|WORKS|stage=6|The demo wire that expired Friday|dataOnly=True|components=[graph,substrate,outcome,corpus,tools,cell,chat]|residual=db.unrouted is per zero-receiver emission with no coalescing named anywhere, so a source firing every minute writes an inbox line every minute; the inbox is an ordinary neuron with a 512-entry ring, which is why the corpus is load-bearing here (Stage 6, not 4). If the cluster is down at ExpiresAt the reminder instant is missed and the tombstone is written on restart — DueAt and ObservedAt make the lag readable, but the wire stays live through the outage. Tombstones never compact and ConnectionsFrom is an O(all connections) deserialize, so a heavily rewired owner pays for their own audit trail on the routing hot path.
S50|WORKS_PARTIAL|stage=4|Who wired Gmail, and who deleted it|dataOnly=True|components=[ingress,chat,tools,graph,identity,view]|residual=The WHO mechanism is real and fully carried for the owner's own principals and the assistant acting on one of them. The scenario's two-person example sits in an undetermined space: the amended spec neither permits nor refuses a cross-principal connect, so the audit answers a question about an act whose legality is unstated. StatedIntent is the one forgeable half. The whole trail is trustworthy against product-surface actors only — cluster membership IS the authority, and there is no asymmetric signing. Pre-rail wires read provenance-absent forever, and tombstones accumulate in the single non-reentrant graph grain whose ConnectionsFrom is a full deserialize.
