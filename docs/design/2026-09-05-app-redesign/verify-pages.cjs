const {chromium}=require('C:/Users/vhorb/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
const path=require('node:path'),fs=require('node:fs');
(async()=>{const browser=await chromium.launch({headless:true,executablePath:'C:/Users/vhorb/AppData/Local/ms-playwright/chromium-1217/chrome-win64/chrome.exe'});const report=[];for(const name of ['index','research']){const page=await browser.newPage({viewport:{width:1440,height:1000}});const errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto(`http://127.0.0.1:8743/${name}.html`);await page.screenshot({path:path.join(__dirname,'previews',name+'.png'),fullPage:true});const links=await page.locator('a[href]').evaluateAll(xs=>xs.map(x=>x.getAttribute('href')).filter(h=>h&&!h.startsWith('http')&&!h.startsWith('#')&&!h.startsWith('D:')));const broken=[];for(const href of new Set(links)){const r=await page.request.get(new URL(href,`http://127.0.0.1:8743/${name}.html`).href);if(!r.ok())broken.push(href);}const desktopOverflow=await page.evaluate(()=>document.documentElement.scrollWidth>innerWidth);await page.setViewportSize({width:390,height:844});await page.screenshot({path:path.join(__dirname,'previews',name+'-mobile.png'),fullPage:true});report.push({name,desktopOverflow,mobileOverflow:await page.evaluate(()=>document.documentElement.scrollWidth>innerWidth),broken,errors});await page.close();}await browser.close();fs.writeFileSync(path.join(__dirname,'pages-verification.json'),JSON.stringify(report,null,2));console.log(JSON.stringify(report,null,2));if(report.some(r=>r.desktopOverflow||r.mobileOverflow||r.broken.length||r.errors.length))process.exitCode=1;})().catch(e=>{console.error(e);process.exitCode=1});
