# Round-1 review findings (critical + major), condensed

[accuracy#1] CRITICAL · highlevel.md · §7 Capability map (paragraph after the table)
PROBLEM: The headline count is wrong. The table itself marks 9 capabilities Missing (C03–C08, C10, C12, C13) and 5 Partial (C01, C02, C09, C11, C15), which is 14 of 15, not 12. Only C14 is Usable. The same paragraph also puts C04 and C05 both among the 'cheap to fix' problems (C01–C05) and among the 'foundations that don't exist yet' (C04–C07), which contradicts itself.
SUGGESTED: Replace the paragraph with: "Fourteen of fifteen capabilities are Missing (9) or Partial (5). Only the neuron runtime (C14) is Usable, and only for one owner on one host. That is the honest shape of \"we miss a lot\". It also explains the order. The experience capabilities C01–C03 fail today on problems that are cheap to fix: the fixed tool list, the hidden prompt suffix, duplicate trace propagation and dead routes. The data capabilities C04–C05 and the ecosystem capabilities C10–C13 depend on foundations (C04–C07) that don't exist yet."

[accuracy#2] CRITICAL · highlevel.md · §3 OS table row 'cron, daemons'; §7 row C11; §8 C11 'Today — Partial (developers only)'
PROBLEM: Behaviors are described as 'dev-only' or 'developers only' today. That is false. The 14 behavior tools are offered on every default /agent turn for every user, and they are the only path for 'draw me a card'. The Behaviors app is hard-coded in every user's shell. current-state.md §1/§4 and C11's own bullet ('End users see xUnit and Orleans logs') say the opposite. Moving behaviors behind developer mode is the target (C11 Target, Phase 0, D3), not today's state.
SUGGESTED: §3 row → "C# behaviors: a developer-grade pipeline that every user gets by default (14 of 16 tools on every turn, Behaviors app in every shell) (A3, B6, B16)". §7 C11 row → "🟡 Partial (developer-grade, on by default for everyone)". §8 C11 heading → "Today — Partial (developer-grade pipeline exposed to every user)". Add the bullet: "Every assistant turn carries the 14 behavior tools. 'Draw me a card' is routed through them."

[accuracy#3] MAJOR · both · highlevel §8 C09 'Today'; current-state §5 'Installed but unreachable' row
PROBLEM: The cause is wrong for 3 of the 4 modules named. ClickHouse, Gmail and GitHub have no assistant tools and no MCP endpoint for the assistant at all. Only Salesforce is blocked by the unregistered MCP bridge, and its 'hosted MCP' is only a config value. The skeptic correction also shows these contracts ARE reachable indirectly, through C# behavior authoring (code_contracts with modules=[] reflects over every host DLL, though the list is likely truncated at 24,000 chars). The Phase 2 exit criterion 'the MCP bridge is registered' would therefore not make ClickHouse, Gmail or GitHub usable. Registering the bridge would not even expose Salesforce by itself, because the workspace turn uses an exact tool-name allowlist.
SUGGESTED: highlevel C09 bullet → "ClickHouse, Gmail and GitHub have no assistant tools. Salesforce's hosted MCP is only an endpoint in configuration, because the generic MCP bridge has no production caller. The assistant can reach any of these contracts only indirectly, by writing a C# behavior, and the unfiltered contract catalog is likely truncated before it lists them. (A3, A12, E4)". Add to C09 Missing: "Tools, or an MCP registration plus allowlist entries, for each connector kept in the product profile." current-state row label → "Installed but not offered as tools (reachable only through C# behavior authoring)".

[accuracy#4] MAJOR · highlevel.md · §10 'Detail: ValueNeuron<T>' last-but-one bullet; §12 D4 'Reverses' column
PROBLEM: The ADR cites the wrong prior decision. The 09-19 spec's 'journals' deletion removed DigitalBrain's own signal journals (Contracts Journals/*, BoundedJournal), not Orleans Journaling. The ratified 09-19 decisions that a journaled tier actually touches are 'Persist module state. Live signals and C# behaviors can restart without replay' and 'Modules use Orleans IPersistentState<TState> directly', along with the deletion of the DurableGrain-based Neuron. Principle 10 depends on ADRs citing the exact decision they supersede.
SUGGESTED: §10 bullet → "This partially reverses the 2026-09-19 decision that modules use Orleans IPersistentState<TState> directly, and its deletion of the DurableGrain-based Neuron. It does not revive the deleted DigitalBrain signal journals. It is recorded as an ADR." D4 Reverses → "Partially reverses 09-19 'modules use IPersistentState directly / DurableGrain Neuron deleted'".

[accuracy#5] MAJOR · highlevel.md · §8 C13 'Today'; §12 D5 'Reverses' column
PROBLEM: Two claims are overstated. First, the 2026-09-22 programmable-behaviors spec is called 'ratified', but its own status line says 'proposed design for implementation'. Second, the spec does not reject a marketplace as a product direction. It only excludes a package marketplace from Stage 2 scope and says contracts 'leave room for these without promising them now'. So D5 does not 'reverse' a 09-22 decision. By contrast, the 09-19 'prefer explicit composition over ModuleManifest' rule is real and ratified.
SUGGESTED: C13 bullet → "The 2026-09-22 programmable-behaviors spec (implemented and verified, though its status line still says 'proposed') excludes a package marketplace from Stage 2 and 'leaves room' for one without promising it." D5 Reverses → "Goes beyond the 09-22 Stage-2 scope (marketplace excluded 'without promising'); partially reverses 09-19 'explicit composition over ModuleManifest'".

[accuracy#6] MAJOR · highlevel.md · §8 C05 Target ('compatible with MCP elicitation forms')
PROBLEM: 'Compatible with MCP elicitation forms' is true only for a flat subset. MCP form mode allows only flat objects of primitives: string with format email, uri, date or date-time, number/integer, boolean, and enum/multi-select. Money, Person, PostalAddress, Location, File, Image and Reference cannot be expressed. The spec also says servers MUST NOT use form mode for passwords, API keys or tokens and MUST use URL mode. So the catalog's Secret type is explicitly incompatible with elicitation forms.
SUGGESTED: Replace the sentence with: "The catalog is published as JSON Schema with x-intochat-* extensions. Its flat primitive subset (Text, Number, Boolean, Date, DateTime, Email, Url, Choice, MultiChoice) maps one-to-one onto MCP form-mode elicitation. Composite types (Money, Person, PostalAddress, Location, File, Image, Reference) need IntoChat's own renderer. Secret is never collected by a form: it maps to MCP URL-mode elicitation or IntoChat's out-of-band secret entry."

[accuracy#7] MAJOR · highlevel.md · §8 C07 Target stage 2 ('Metering middleware in the shared chat-client pipeline covers every LLM call site'); §11 Phase 2 exit
PROBLEM: This leaves out a verified precondition. The main spend path, the /agent AgentTurnRunner, does not use the shared pipeline. It builds its client with ModelProfiles.CreateInferenceClient, which never calls AIClients.BuildChatPipeline. Middleware placed in the 'shared pipeline' would therefore meter everything except assistant turns. That is also why the agent emits no GenAI spans.
SUGGESTED: Add as the first item of stage 2: "Route AgentTurnRunner's model client through the same pipeline as every other call site. Today it uses ModelProfiles.CreateInferenceClient, which skips AIClients.BuildChatPipeline, so it has no OpenTelemetry and no place to meter. Then add the metering middleware to that one pipeline, covering AgentTurnRunner, InferenceService, PlaywrightWebAgent and UntrustedContentScreen." Add to the Phase 0 exit: "The agent client goes through BuildChatPipeline."

[accuracy#8] MAJOR · highlevel.md · §10 Answers to the owner's ideas (IAW row and the vector-index row)
PROBLEM: The owner asked us to investigate IAW's 'UI neuron that drew UI in Telegram' and its 'vector DB with all agents'. The document never states what those actually were, so the owner's premise goes uncorrected. IAW's agent 'vector DB' was never a vector DB. It was an in-memory Dictionary in a plain grain (the spec said Qdrant). It indexed whole agents only, not tools. It rebuilt without embeddings on every reactivation, so after 15 idle minutes it silently became keyword-only. IAW also had no UI neuron: UI was inferred after the fact on the Telegram side by regex plus an LLM reformatter. Only options, suggestions, media and forward were ever rendered, and Card, Form and Progress parts were never rendered.
SUGGESTED: IAW row 'In short' → prepend: "What IAW actually had: no UI neuron. A Telegram-side formatter inferred buttons from the LLM's text with regex, plus an LLM fallback. Only options, suggestions and media were ever rendered." Vector row → append: "IAW's 'vector DB of agents' was an in-memory grain (never Qdrant) that indexed whole agents, not tools. It silently fell back to keyword-only after 15 idle minutes. Those failure modes are acceptance criteria here too (G5, G6)."

[accuracy#9] MAJOR · highlevel.md · §13 Risk 3 'Churn and re-litigation'; §8 C13 'Today'
PROBLEM: The churn history leaves out the most relevant precedent. A marketplace itself was already built in this repo and removed. Commit 3354733d3 (2026-07-08, 'Remove marketplace distribution path') deleted a 566-line marketplace_screen.dart and a Pack model. The Projects archive (research H1) holds 7+ earlier from-scratch attempts at the same 'neurons + marketplace that installs bundles' vision. C13 presents the marketplace only as never started.
SUGGESTED: Risk 3 → "...The capability index was built and deleted twice, BDD about five times, a marketplace distribution path (store screen plus pack model) was removed on 2026-07-08 (3354733d3), and docs were deleted on 09-11 and 09-17." C13 Today: add the bullet "A marketplace screen and pack model existed and were removed on 2026-07-08. Earlier from-scratch attempts in E:\\intochat\\Projects never tested install-to-dispatch end to end (H1)."

[owner-fidelity#1] CRITICAL · highlevel.md · §10 Answers to the owner's ideas; §6 Non-goals; §12 Decisions
PROBLEM: The owner cited E:\intochat\Projects\docs\projects-survey-comparison.md for the application format. That document and CONTINUATION.md invariant 3 recommend the opposite of this plan: a distribution-first, signed typed-C# pack installed into the running cluster, then Roslyn compile, collectible ALC, and registration as a grain, with kernel updates shipped through the marketplace. The plan silently overrides this. A non-goal forbids loading third-party assemblies into the silo and bans new grain types, and the marketplace is pushed to Phase 4–5. The survey is never named, and the question 'May apps bring their own neuron types?' (H19) is settled inside a non-goal instead of being put to the owner as a decision. The tested prior assets the survey points to (final's 16 Reqnroll distribution scenarios, Ed25519 signing, v3 GateNeuron 'verify before activate', and old digitalbrain's ECDSA+licence+Stripe) are not considered for reuse.
SUGGESTED: Add this row to §10: "| Signed typed-C# packs embodied in the running cluster (projects-survey-comparison.md; Projects/CONTINUATION.md invariant 3) | **reject for third-party code; keep the tested parts** | The survey's keystone vertical (signed pack → install into the running cluster → Roslyn compile → collectible ALC → register as a grain) runs publisher code inside the silo. .NET has no in-process security boundary (I7), so third-party and assistant-written code runs out of process behind the broker, and apps cannot add grain types (D16). First-party modules keep code-first `WithModule` composition and ship by host deploy, which also drops 'kernel updates through the marketplace'. Reused from the survey: signed packs (final's Ed25519, old digitalbrain's ECDSA + licence), verify-before-activate (v3 `GateNeuron`), and final's 16 Reqnroll distribution scenarios as the template for certif

[owner-fidelity#2] MAJOR · highlevel.md · §10 Answers to the owner's ideas
PROBLEM: §10 is the place the owner will look for answers, but it has only 9 rows. Several explicit asks have no row: the confusing and trash-filled 'Show me all customers' flow, the date-card failure, SDK data types for date of birth and password, grain vs durable grain segregation (answered only in the detail paragraph), 'how an app tells what it does (neuron interface?)', and the EM/PM deliverable. The answers exist, scattered across current-state §3–4, C02, C05, C10 and C14, but nothing traces each ask to where it is answered.
SUGGESTED: Add these rows to §10: "| 'Show me all customers' is unclear; a lot of trash | explained, then simplified | current-state §3 walks the path. It is sound (read-only transactions, server paging), but it carries a hidden prompt suffix, 14 unrelated tools, a model that never sees its table, and dropped turns. Phase 0–1 remove these (C01, C09); J1 is the target. |" · "| 'Draw me a card …' failed on 'date' | root cause found; three fixes | The allowed kinds are hidden inside the grain, so the model guessed (current-state §4). Fixes: typed field kinds in contracts (C05), a declarative form tool with no compile step (C02), and a path into the workspace. J2 is the acceptance test; D3 decides. |" · "| SDK data types: store and reuse my date of birth, a password | adopt | Semantic type catalog (C05), the My Data vault and `SecretRef` (C04). Phase 1. |" · "| Grain vs durable grain segregation | adop

[owner-fidelity#3] MAJOR · highlevel.md · §10 row 'IAW's UI neuron'; C08
PROBLEM: The owner asked for an investigation of IAW's UI neuron and its 'vector DB with all agents'. §10 reports only the UI half. IAW's agent registry was researched in G5–G7: interface-as-manifest with routing examples, 0.6 cosine + 0.4 keyword ranking, top-5 agents injected per turn, an in-memory grain rather than Qdrant, embeddings silently lost after 15 minutes idle, and only agents indexed, never tools. None of this is reported, and agents or specialists are not among C08's collections. Other IAW lessons the plan relies on, including the once/thread/always approval scopes, the memory provider with provenance, the typed integration agents and the durable-grain-on-volatile-storage warning, are adopted without credit, so the owner cannot see what the IAW investigation concluded.
SUGGESTED: Add these rows to §10 below the IAW UI row: "| IAW's vector DB of all agents | adopt the pattern, not the implementation | IAW declared each agent's name, description, capabilities and routing examples on its C# interface, ranked agents by 0.6 × cosine + 0.4 × keyword, and injected the top 5 each turn (G5). It was an in-memory grain, not Qdrant. After 15 idle minutes it rebuilt without embeddings and silently became keyword-only, and it indexed agents but never tools (G6). We keep interface-as-manifest plus routing examples and fix the failures in C08: persistent collections, re-embedding on content-hash change, a visible degraded state, and indexing at app, operation and agent level. Specialists become indexed agent definitions or are removed (C01). |" · "| IAW's approver (once / this thread / always) | adopt the scopes, drop the LLM judge | C06's grant scopes come from IAW's ApproverAg

[owner-fidelity#4] MAJOR · highlevel.md · §9 J2 and J5; §12
PROBLEM: J2 merges two separate owner asks. One is a card with name, surname and date-of-birth inputs, which is a form collecting anyone's data. The other is 'store and reuse MY date of birth'. J2 answers the form save with 'Keep date of birth in My Data?', which would push a customer's date of birth into the owner's personal vault. J5 then saves the same card as 'Customer intake', which confirms it collects other people's data, so J2 and J5 contradict each other. The plan never decides where typed business data (customer DOBs in forms or tables) lives or what governs it; research K left this open.
SUGGESTED: Replace the J2 Gherkin with: "When I ask for a card with name, surname and date of birth / Then a form window appears within 10 seconds with a date picker, and nothing is compiled / When I fill it in and press Save / Then the entry is stored in the card's own data as Text, Text and Date (sensitivity: Personal) / And the assistant sees the field types and a handle, not the values (D18) / When I add a password field / Then the input is masked, the card shows '•••• set', and the assistant sees only a handle". Add a second scenario: "When I say 'remember my date of birth' and enter it / Then it is stored in My Data as me.birthDate / And an app reads it only after I grant (app, person.birthDate, read)". Add to §12: "| D17 | Where typed business data lives | Form and table records belong to the workspace and the app that holds them, governed by workspace scope and app grants. Only the owner's 

[owner-fidelity#5] MAJOR · highlevel.md · C07 Compute; §10 row 'Per-neuron tariffs'; §11 Phases 2–3
PROBLEM: The owner gave a concrete example: a filesystem that stores files in blob storage and charges for it. The plan names `storage.gb_month` but no phase, exit criterion or journey delivers a storage meter. It never reconciles 'local operations are free' with the example. Today's Files app browses the host's Downloads folder, and D2 moves to a hosted deployment, where Files would have to become a blob store. It does not say who sets a neuron's price (the platform, or the publisher with review). It also puts 'app-function and storage meters' in the broker, which arrives in Phase 4, while Phase 3 already charges for BackgroundRemover's per-image function calls, so the Phase 3 metering point is undefined.
SUGGESTED: Add to C07 Target after stage 2: "2b. **Storage meter (first-party).** Under D2 (hosted), Files stores content in blob storage and meters `storage.gb_month` (average of daily peaks). The meter is shown in shadow on receipts from Phase 2 and charged from Phase 3. Files over a local disk on the owner's own machine stays free." Add: "**Who sets prices.** IntoChat prices first-party meters (provider list cost plus the D8 margin). A publisher proposes prices for its app's meters; IntoChat reviews and locks them per version, and any increase triggers re-consent." Replace ':516-518 App-function and storage meters live in the broker' with: "Until the broker exists (Phase 4), first-party function and storage meters are recorded by the Phase 2 caller-context call filter (C06) into the same durable meter log. The broker takes over the same meter ids." Add to the Phase 2 exit criteria: "A hosted-fil

[owner-fidelity#6] MAJOR · highlevel.md · §9 J3; §12 D14; §10
PROBLEM: The owner's example is 'LeadGenerator from the marketplace'. The plan makes it a first-party declarative app, and §10 has no row explaining why it is not a marketplace app. J3 never says where leads come from. A declarative app can only compose installed capabilities, yet no capability in C09 finds companies. The web tools that could (Tavily websearch, browse_web, lookup_company) are registered but never offered to chat, and they appear nowhere in the plan. D14 forbids LinkedIn-derived fields and defers Firmledger with no alternative, so J3 cannot be tested as written.
SUGGESTED: Add under J3: "LeadGenerator v1 composes web search and company lookup (the registered `websearch`, `browse_web` and `lookup_company` tools, which are not offered to chat today), a Leads live table held in the app's own data, and a daily automation. It never uses LinkedIn-derived data." Add this row to §10: "| LeadGenerator from the marketplace | adopt, staged | Phase 2 ships it first-party through the same manifest, consent and install path a marketplace app uses, because third-party trust (broker, certification, payouts) arrives in Phase 4. Firmledger, wrapped as a `remote` app after counsel review, is the first candidate for a third-party lead generator in Phase 4. |". Extend D14's recommendation with: '; Firmledger as the first invited `remote` app candidate in Phase 4'.

[owner-fidelity#7] MAJOR · highlevel.md · §11 Phase 5; §6 Non-goals; C13; D6
PROBLEM: The owner's marketplace is for apps made 'by developers or regular people'. The plan says declarative apps contain no code, 'the platform runs it', and puts creators' declarative apps in ring 3, before sandboxed code apps. Phase 5 nevertheless groups non-programmer publishing with sandboxed code apps and gates both on 'a penetration test of the cross-platform sandbox'. The non-goal also blocks all public publishing until a sandbox exists. Declarative apps never need a sandboxed executor, so the regular-people half of the ask is delayed to the last, unscheduled phase for no stated reason.
SUGGESTED: Split Phase 5 into: "### Phase 5a — Creators publish · appetite 6 weeks · C13 C10 C08. Non-programmers publish saved declarative apps. Gated by identity, the broker, certification of scenarios on fakes, the kill switch and re-consent on permission growth. No sandbox is needed because declarative apps carry no code." and "### Phase 5b — Sandboxed code apps · later. Developers ship `process` apps as signed packages, gated by a penetration test of the cross-platform sandbox." Reword the non-goal to: "Public self-serve publishing of code apps before the sandbox exists, or of any app before identity, the broker and the ledger exist."

[owner-fidelity#8] MAJOR · both · current-state §3; highlevel J1
PROBLEM: The owner said the customers flow is unclear and contains 'a lot of trash'. current-state §3 lists what happens but not why each step exists, or which steps are essential and which are trash. It does not explain the 1–32 column limit, the LIMIT 0 describe, the retry loop or the fingerprinted operation grain. highlevel gives only a Gherkin outcome for J1 and no target step list, so the owner cannot see which of today's steps survive.
SUGGESTED: Add to current-state §3: "### What each step is for, and what happens to it
| Step today | Why it exists | Verdict | Where |
|---|---|---|---|
| Client appends ~1 KB of hidden context | carries specialist, artifact and behavior selection | delete: send structured fields; never store them as user text | C01, Phase 0 |
| Same 16 tools and a 1,324-char prompt every turn | nothing selects tools | change: at most 8 tools per intent; no behavior tools by default | C01 |
| `supabase_schema` reads information_schema from the provider | gives the model table and column names | keep, behind the live-table source | C09 |
| `QueryWindowOperation` records the call by fingerprint, then `ISupabaseTable.CreateFromQueryOnce` | a retried call reuses one window | keep the idempotency; fold into one live-table open | C09 |
| Regex guard | fast first filter | keep as a filter only | — |
| READ ONLY transacti

[owner-fidelity#9] MAJOR · current-state.md · §1 The product in one paragraph (missing system map)
PROBLEM: The owner's first complaint is 'it's unclear how it works'. current-state explains one flow and one failure, but never shows what runs where: which processes, containers, external services and stores exist, and how the Flutter client, the IntoChat process and silo, the behavior workers and the modules connect. A newcomer still cannot draw the system.
SUGGESTED: Insert '## 1a. What runs where' after §1: "| Piece | What it is | Where it runs |
|---|---|---|
| Aspire AppHost | starts and wires everything; picks modules with `WithModule<T>()` at compile time | developer machine |
| IntoChat | one .NET process: HTTP API + SSE for the client, plus an Orleans silo hosting all 14 modules and the `TestTwitterModule` demo; serves the Orleans dashboard | started by the AppHost |
| Flutter shell | the workspace client (chat + windows); talks HTTP/SSE to IntoChat; keeps its own copy of projects and chats in local storage | desktop / web |
| Behavior workers | one Windows process per behavior run, joining the silo as an Orleans client | host machine |
| Azurite | grain state blobs, the unused journal container, the reminders table | container |
| Qdrant | Memory module vector store (no data volume, no consumers) | container |
| ClickHouse | database seeded w

[owner-fidelity#10] MAJOR · both · current-state §5 The clutter; highlevel §11 Phase 0 exit criteria
PROBLEM: 'A lot of trash' is a direct owner ask, and principle 2 says every epic names what it deletes. Yet several clutter items in current-state §5 and the research have no disposition in any phase: the public handbook (digitalbrain.docs), README and continuation.md, stale spec status lines, CI and deploy paths pointing at the old Flutter folder, the name collisions and the `intocaht` storage key, the behavior MCP endpoint, the 15 REST routes and the two listening APIs, the empty Aspire Contracts project, the orphaned `digitalbrain.capabilities` guard and Reqnroll .gitignore rule, the duplicate ProcessRunner, and the Files app exposing the host Downloads folder by default. The owner cannot check that the trash will actually go.
SUGGESTED: Add to highlevel §11 before Phase 0: "**Trash register** (every row in current-state §5 has an owner):
| Clutter | Disposition | Phase / epic |
|---|---|---|
| 15 compile-excluded files; ConversationalAgent; Kernel MCP; empty Aspire Contracts project | delete | 0 · Clear the desk |
| 'New work' items, /programs, artifact editors, table import, brain graph (~3,150 lines Dart) | delete | 0 · Clear the desk |
| Voice button → missing route | rewire or delete (D13) | 0 |
| TestTwitterModule, ElonBitcoin, InboxBanner polling | move to a test host / delete | 0 |
| CI and deploy paths to src/Modules/Flutter | fix | 0 · Tell the truth |
| CONTEXT.md, README, continuation.md, spec status lines, public handbook | rewrite, or archive with a 'superseded' banner | 0 · Tell the truth |
| Orphaned `digitalbrain.capabilities` guard, Reqnroll .gitignore rule, duplicate ProcessRunner | delete (or restore 

[owner-fidelity#11] MAJOR · both · highlevel C04 Today, D13, Phase 0; current-state §6
PROBLEM: The plan reverts the owner's latest commit (739c6c385, pushed minutes before these docs) without acknowledging why the owner made it. The commit's comment says it exists 'so GenAI traces in Aspire show the actual conversation and tool activity', which is the owner's trace complaint. The plan also never tells the owner that the commit does not achieve that goal: the /agent path uses an uninstrumented client, so no conversation content appears for it. highlevel C04 states flatly that it 'captures full prompts'. The owner's real need, seeing what the assistant did, is replaced by 'opt-in per session' with no explanation.
SUGGESTED: In C04 Today, replace 'Commit `739c6c385` captures full prompts in dev telemetry.' with: "The owner's commit `739c6c385` turns on full prompt and tool capture in dev runs so traces show the conversation. It records content only at instrumented LLM call sites. The `/agent` path uses an uninstrumented client (D9), so the conversation the owner wanted to see still does not appear. Once C03 instruments the agent, the same flag would put dates of birth, passwords and customer rows into traces." Change the D13 recommendation to: "Keep the owner's goal and remove the leak: in dev, content capture stays on for the local owner, with values of Personal and Credential semantic types redacted; it is off for any other principal and in every hosted deployment." Change the Reverses cell to 'Modifies `739c6c385`'. Change the Phase 0 bullet at :954 to match.

[owner-fidelity#12] MAJOR · highlevel.md · §7 Capability map
PROBLEM: The owner asked for 'ALL product capabilities with an overview'. The map lists platform capabilities but leaves out user-facing and operational ones. There is no first-party app portfolio: Files, Image Editor, the 'Image Studio' named only in Phase 3, Notes/Tables/Drive from the customer design, LeadGenerator, and Behaviors. There is no web research (Tavily search, browse, company lookup, all registered but unused), which LeadGenerator needs. There is no Inbox or notifications (IInbox is a volatile list fed by a demo), no client platforms (desktop, web, mobile; the doc has IAP non-goals), and no hosting and operations, even though D2 commits to a hosted deployment per design partner. Voice appears only in D13.
SUGGESTED: Add these rows to §7 (and a short §8 entry each): "| C16 | `first-party-apps` | First-party apps: Files, Image Editor (→ Image Studio with BackgroundRemover), Live tables, LeadGenerator; Notes and Drive deferred | Bundled apps | 🟡 Partial (Files, Image Editor, Behaviors) | now |" · "| C17 | `research` | Web research: search, browse, company lookup | Browser | 🔴 Missing from chat (tools registered, never offered) | next |" · "| C18 | `notifications` | Inbox, notifications and background-run status | Notification centre | 🔴 Missing (IInbox is 50 volatile strings fed by a demo) | next |" · "| C19 | `operations` | Hosting, deployment, backup, clients (desktop, web, mobile) and support | Installer + system services | 🟡 Partial (Aspire local, deploy.yml with stale paths) | now → next |". Change the sentence at :273 to count the new rows.

[owner-fidelity#13] MAJOR · highlevel.md · C05 Semantic data types; §10 ValueNeuron detail
PROBLEM: The owner's ask was developer-facing: 'the SDK should provide data types … Text : ValueNeuron<string>'. C05 answers at product level (a JSON Schema catalog) and dispatches the developer side in one clause ('Contracts use enums and value types'). It never shows what a developer writes, whether apps can define their own records (LeadGenerator's Lead, BackgroundRemover's image job) from catalog types, or how a closed catalog of about 22 types serves a marketplace. It says to 'rename away from Text' but proposes no name.
SUGGESTED: Add to C05 after the catalog paragraph: "**For developers (the SDK answer).** `DigitalBrain.Contracts` ships the catalog as C# value types (`Date`, `Email`, `Money`, `Url`, `SecretRef` …) with validation built in. A contract takes an enum (`FieldKind.Date`), never a free string, and a neuron's state holds `Date BirthDate`, never `string`. The catalog is closed for primitives and formats and open for composition. An app declares its own records whose fields use catalog types (e.g. `Lead { Company: PlainText, Website: Url, Email: Email, City: PostalAddress }`), and those records flow into its manifest, tool schemas and consent sheet. A new primitive or format needs a catalog ADR. The owner's `Text` becomes `PlainText` (proposed), so it does not collide with the UI kit's `text` kind."

[owner-fidelity#14] MAJOR · highlevel.md · §12 Decisions needed from the owner
PROBLEM: The owner is asked to ratify 15 decisions, but each row gives only a recommendation and what it reverses. The rejected options are not shown. That breaks the ADR and opportunity-tree practice the research recommends and leaves the owner nothing real to choose between. It matters most for D1–D5, which the doc says 'decide everything else'.
SUGGESTED: Add an 'Options considered' column, filled at least for D1–D5: D1: "(a) operator who is also a self-creator [rec]; (b) prosumer files/images (the five-app customer design); (c) developers-first marketplace; (d) Salesforce B2B wedge (August strategy)". D2: "(a) hosted, one deployment per design partner [rec]; (b) local-first desktop, single owner; (c) multi-tenant cloud now". D3: "(a) declarative by default [rec]; (b) keep C# behaviors, adding typed contracts and a test brain; (c) both, behind a router". D4: "(a) typed fields of entity neurons, three tiers [rec]; (b) `ValueNeuron<T>` per value; (c) every neuron journaled". D5: "(a) manifest first, three kinds [rec]; (b) signed C# pack embodied in the silo (survey); (c) behaviors-only single-file artifacts".

[owner-fidelity#15] MAJOR · highlevel.md · §6 Principle 10; §11 Phases
PROBLEM: Principle 10 caps work at 2 epics in progress, but Phase 1 lists 7 candidate epics in a 6-week appetite and Phase 2 lists 8 in 8 weeks. Each epic would then average under 2 weeks, even though epics like 'Semantic types v0', 'Secrets by reference & My Data v0' and 'Workspace scoping' are large. No capacity assumption is stated (the owner plus how many AI agents), although risk #1 is 'a team of one'. As written, the roadmap breaks its own WIP rule.
SUGGESTED: Add to §11 intro: "**Capacity assumption.** One owner plus AI coding agents; an epic takes 1–3 weeks. With WIP ≤ 2, a 6-week phase fits at most 4 epics." Merge the Phase 1 epics into 4: *Assistant that sees its tables* (One assistant runtime + Live tables the assistant can read) · *Instant typed forms* (Semantic types v0 + Instant UI) · *Safe by default* (Secrets by reference & My Data v0 + Workspace scoping) · *Receipts with shadow cost*. For Phase 2, either extend the appetite to 12 weeks or move *Customer automations* and *Persistence tiers* to Phase 3.

[owner-fidelity#16] MAJOR · highlevel.md · §1–§2 (vision); §11 (prioritisation)
PROBLEM: The owner's vision is a new operating system with a marketplace. The doc has no end-state vision statement with a horizon and business goals: the press release describes the v1 operator product, and the OS appears only as an architecture frame. There is also no prioritisation method. The phase order is argued in prose, with no cost-of-delay or WSJF-style comparison and no trace from owner pains (opportunities) to epics, so the owner cannot see why, for example, Discovery waits for Phase 2 while Receipts come in Phase 1.
SUGGESTED: Add '### Vision (2028)' at the top of §2: "IntoChat is the operating system where people get work done by asking. An assistant finds, installs and runs the right app from a marketplace built by developers and by regular users. Every app declares what it does, what data it touches and what it costs in Compute, and every run ends with a receipt. Business goals: weekly accepted intents per workspace (north star), share of intents served by apps not made by IntoChat, number of paying publishers, and gross margin per intent." Add to §11 a 'Why this order' table with columns: Phase | Owner pain addressed (customers flow, date card, trace trash, DOB/password trust, marketplace) | Outcome moved (O1–O5) | Cost of delaying one phase.

[owner-fidelity#17] MAJOR · highlevel.md · C03 Receipts, activity and traces; §11 Phase 1 exit
PROBLEM: Receipts are the core customer promise ('Every request ends with a receipt'; O1: 100% of intents), yet C03 says 'the user-facing receipt is built from the trace plus the metering ledger'. The same section adds tail sampling, and today's dashboard evicts a trace after about 47 minutes. Principle 8 says telemetry is never a source of record. Phase 1 requires receipts with shadow cost before durable metering exists (Phase 2), so in Phase 1 receipts could only be read from telemetry.
SUGGESTED: Replace :370 with: "The receipt is a durable record per intent id. The platform writes it when the intent ends, from the agent run log, tool and app calls and meter events, and stores it with the conversation. Traces link to it for diagnosis but are never its source, because they are sampled and evicted." Change the Phase 1 exit bullet to: "Every intent has a durable receipt; shadow Compute on it comes from the recorded token usage (persisted since Phase 0), not from spans."

[owner-fidelity#18] MAJOR · highlevel.md · C09 Connections; J1; §12
PROBLEM: J1 and C09 commit the assistant to reading the table it opened ('table_read', 'bounded answers in chat', '312 in London'). Customer rows are personal data, which principle 5 says flows only under grants, and C01 says the model channel carries only 'summary plus handle'. Research A asked this explicitly (exposure versus token cost), but no decision covers what the model may read from connected data. The plan decides it implicitly and inconsistently.
SUGGESTED: Add to §12: "| D18 | What the assistant may read from connected data | By default: schema, row counts and aggregates. Row values only for columns whose semantic type is Public, or under a per-connection grant. Credential-typed values never. Every row read is counted on the receipt. | — |". Append to J1: "And the assistant saw only the count and the column names, not the rows (D18)".

[pm-quality#1] CRITICAL · highlevel.md · §5 Outcomes and north star
PROBLEM: The north star cannot be computed. 'Accepted', 'kept', 'first try' and 'no repair prompt' have no working definition. No capability owns product-analytics events. The north star has no baseline, no target and no date, and with one user through Phase 2 it is measured on a sample of one.
SUGGESTED: Insert after line 186: "**Measuring it.** An intent counts as *accepted* when, for its `intent id`: (1) it ended `succeeded` with no platform, tool-contract or timeout error; (2) the user did not retry, regenerate or send a correcting message for the same intent within 5 minutes; (3) the result was kept: the window stayed open ≥ 60 s, or was saved, pinned or refined, or a chat answer got no thumbs-down; (4) actual Compute ≤ the approved cap. The platform writes content-free product events (`intent.started|completed|failed|kept|repaired|rated`, `app.saved|installed|run`) to a durable, unsampled analytics log keyed by intent id and workspace. These events are never derived from traces or the billing ledger. An *active workspace* has ≥ 1 accepted intent in the week. **Baseline:** measured on the owner's workspace for the 2 weeks after Phase 0. **Targets (proposed):** ≥ 60 % of intents accep

[pm-quality#2] CRITICAL · highlevel.md · §4 Who it is for / §6 Non-goals / D1
PROBLEM: The v1 primary persona has 'data in Supabase, Salesforce or spreadsheets', and the PR promises 'without writing code'. But spreadsheets have no connector: Excel was dropped and CSV import is an explicit non-goal. Salesforce becomes reachable only in Phase 2. So Phases 0-1 serve only Supabase users, who are usually developers, not SMB owners. Four of the eight personas are marked primary, including two internal ones (the assistant, the platform operator), so 'primary' no longer sets focus.
SUGGESTED: Replace the first cell of line 167 with: "**Business operator**: an ops or RevOps lead at a 5–50-person company whose operational data is already in Postgres/Supabase (Phase 1) or Salesforce (Phase 2). Businesses that run only on spreadsheets are outside v1. The owner is user #1, then 3 design partners who match this profile." Change the v1 cell of 'The assistant' to "design persona" and of 'Platform operator' to "internal". Then pick one: (a) add a non-goal "Spreadsheet and CSV sources in v1 (the Excel module was removed in 4bd2ac027)", or (b) delete "or CSV import" from line 246 and add to C09 Missing "Google Sheets / CSV connector (Phase 2)". Restate D1 with the chosen profile.

[pm-quality#3] CRITICAL · highlevel.md · §9 Key journeys (J2, J3) vs §11 Phases
PROBLEM: Two journeys need capabilities that are scheduled after their own phase. J2 (Phase 1) asks 'Keep date of birth in My Data? Apps need your OK to read it'. That needs the grant model and consent sheet (C06) and apps (C10), and neither is in Phase 1. 'When I save it' is also ambiguous: it could mean submitting the values or Save as app (J5, Phase 2). J3 (Phase 2) shows 'estimated Compute and a monthly cap' on the consent sheet. Install consent with a cap is D9 level 2, which the 'Approvals & allowances' epic delivers in Phase 3, and caps need the wallet (C07 stage 5, Phase 3). As written, Phase 2 cannot pass J3.
SUGGESTED: J2: replace lines 849-851 with "When I fill it in and press Save\nThen my date of birth is stored in My Data as a Date and shown in the My Data app\nAnd only this card can read it until I grant access", and move the grant prompt to J5 (Phase 2). Add a Phase 1 exit bullet: "My Data v0 stores typed values from forms; access is limited to the window that owns them." J3: replace line 861 with "And a consent sheet shows its examples, the data types it uses, its permissions and an estimated Compute cost (shadow, not charged)", and move the monthly-cap clause into J4. If the cap must stay in J3, instead add to the Phase 2 exit "Install consent with a per-app monthly cap is enforced by the platform call filter" and move the 'Approvals & allowances' epic to Phase 2.

[pm-quality#4] CRITICAL · highlevel.md · Missing section (after §13 Risks)
PROBLEM: The plan rests on untested bets but has no assumption register and no validation plan. The research recommended one (Torres assumption tests, SAFe leading indicators). §13 lists only risks. O2's '≥ 90 % declarative' and the discovery target of ≥ 0.9 are asserted, although the index failed twice.
SUGGESTED: Add "## 13a. Assumptions and how we test them" with the table: | # | Assumption | Cheapest test | Pass signal | Decide by | / A1 Operators will let an assistant query production business data | 10 problem interviews + a concierge J1 on a partner's read-only role | ≥ 5 of 10 agree to connect | Phase 0 end / A2 People want to keep results as apps rather than ask again | Count repeated identical intents; a fake-door 'Save as app' button in Phase 1 | ≥ 30 % of repeats click Save | Phase 1 end / A3 Declarative forms and views cover ≥ 90 % of 'show/draw' requests | Label 50 real requests against catalog v0 | ≥ 45/50 expressible | before the Instant UI epic / A4 Discovery reaches top-5 recall ≥ 0.9 off the hot path | 2-day spike on 60 golden prompts over first-party manifests | recall ≥ 0.9, p95 < 300 ms | before the Discovery epic / A5 B2B operators accept prepaid Compute rather than a subscri

[pm-quality#5] MAJOR · highlevel.md · §11 Phases / D1 / §13 risk 1
PROBLEM: Design partners appear only in the Phase 3 exit, roughly 16 weeks in. Phases 0-2 are validated only by the owner, a developer who does not match the ICP, even though risk 1 is 'nobody yet delegates real work every week'. The Phase 4 exit ('5 invited apps used weekly by workspaces other than their authors'') needs more demand than 3 partners plus the owner, and no phase grows demand. D2's dedicated deployment per partner also means one catalog must install into separate deployments, which is never stated.
SUGGESTED: Add to the Phase 0 exit: "10 problem interviews with target operators; 3–5 design partners have signed a letter of intent that names their data source." Add to the Phase 1 exit: "≥ 2 design partners run J1 on their own data every week (shadow cost, free)." Add to the Phase 2 exit: "≥ 3 design partners each keep ≥ 1 saved app or automation that re-runs weekly." Add a Phase 4 entry criterion: "≥ 15 active paying workspaces." Add the candidate epic "*Design-partner program & growth*" to Phase 3. Append to D2: "Invited remote apps are listed in one central catalog and installed into each dedicated deployment."

[pm-quality#6] MAJOR · highlevel.md · §11 Phase 0 exit / §5 O1
PROBLEM: The Phase 0 exit 'Narration test passed for J1' uses O1's definition, 'from this page plus one receipt'. Receipts are Missing and arrive only in the Phase 1 exit, so Phase 0 cannot pass its own criterion.
SUGGESTED: Replace line 958 with: "Narration test passed for J1, using this page plus the intent's single trace (the receipt UI arrives in Phase 1)." Change O1's narration clause to: "...from this page plus the intent's trace (Phase 0) or its receipt (Phase 1 onward)."

[pm-quality#7] MAJOR · highlevel.md · §7 Capability map (Horizon) vs §11 Phases / §16
PROBLEM: Horizons (now/next/later) have no stated mapping to phases, yet §16 applies the rule 'only now epics get features and work items'. The next steps imply now = Phase 0-1 and next = Phase 2. On that reading, C06 'now → next' is wrong (accounts and allowances are in Phase 3), C07 'now (shadow) → next' is wrong (wallet and top-ups are in Phase 3), C09 'now' ignores the Phase 2 token vault and MCP bridge, and C14 'now' ignores the Phase 2 persistence tiers. There is also no dependency view, which is how the J2, J3 and Phase 0 contradictions got through.
SUGGESTED: Insert after line 271: "**Horizon ↔ phase.** `now` = Phase 0–1, `next` = Phase 2, `later` = Phase 3+. A capability that spans horizons shows its first and last." Change the cells: C06 `now → later`, C07 `now (shadow) → next (metering) → later (wallet)`, C09 `now → next`, C14 `now → next`. Under each journey in §9, add a 'Needs:' line listing every capability and the phase that delivers it, e.g. "J3 needs: C10 (P2), C08 (P2), C11 (P2), C06 install consent (P2), C07 shadow estimate (P1)".

[pm-quality#8] MAJOR · highlevel.md · §6 Principle 10 / §11 Phase 1 and Phase 2 candidate epics
PROBLEM: 'At most 2 epics in progress' with 7 candidate epics in Phase 1's 6-week appetite and 8 in Phase 2's 8 weeks leaves about 1.7-2 weeks per epic, which contradicts the '2-3 epics per quarter' guidance. These are features dressed as epics, and the WIP limit and appetite cannot both hold.
SUGGESTED: Replace the Phase 1 candidate epics with: "*Ask & see (J1)*: one assistant runtime, live tables with read and refine, receipts with shadow cost · *Draw & keep safely (J2)*: semantic types v0, instant UI, secrets by reference & My Data v0 · *Workspace scoping* (enabler). The other names become features." Replace the Phase 2 candidate epics with: "*Make it yours (J5)*: app format v1, Save as app, customer automations · *Find & install (J3)*: discovery, principals & caller context, LeadGenerator · *Durable foundations* (enabler): durable metering, token vault & MCP connectors, persistence tiers."

[pm-quality#9] MAJOR · highlevel.md · §7/§8 Capability map (missing Inbox and notifications)
PROBLEM: No capability covers work that reaches the user while they are away: automation results and failures (J3 'every morning'), 75/90/100 % budget alerts (C07), pending approvals that 'survive restarts' (C06), expired connections, and kill-switch notices (C13). The plan deletes the InboxBanner and puts nothing in its place. The earlier customer design had a structured Inbox, and this plan drops it without a decision.
SUGGESTED: Add the row "C16 | `inbox` | Inbox and notifications | Notification centre | 🔴 Missing (strings-only `IInbox`, demo banner) | next" and a section: "**What it is.** Where work that happened while you were away reaches you: an automation result is ready, a run failed, an approval is waiting, a connection expired, a budget reached 75/90/100 %, an app was switched off. **Target.** Structured items (id, kind, source intent/app/window, actions, read/resolved state); repeats grouped; a deep link to the exact window or receipt; pushed over SSE; an email digest for approvals waiting more than 1 h. **Missing.** A structured `IInbox`, and producers in automations, Compute alerts, approvals, connections and the kill switch." Append to J3: "And each morning's result appears in my Inbox, and a failed run creates an Inbox item with the first error." Add C16 to the Phase 2 capability list and exit crite

[pm-quality#10] MAJOR · highlevel.md · §1 Press release 'Getting started' / C02 / C09
PROBLEM: The PR says 'open a workspace, connect a data source, ask', but no capability or phase delivers a first-run experience, a self-serve way to create a workspace, or a user-facing connect flow. Today the Supabase connection string is an operator-side Aspire secret parameter. C09 lists token-vault references but no connect UI or connection states. The earlier customer design specified a first-run flow and window states, and this plan drops both.
SUGGESTED: Add to C09 Target: "A self-serve **Connect** flow: pick a source; paste a connection string or sign in with OAuth (entered as a `SecretRef`); test it with a read-only probe; see its status (connected / expired / failing); disconnect." Add to C02 Target: "A first-run flow: a new workspace opens the assistant with 3 starter prompts that match the connected sources, and every window has loading, empty, permission-denied, expired-connection and failed states." Add to the Phase 2 exit: "A design partner goes from invitation to a first accepted J1 intent in ≤ 15 minutes without operator help."

[pm-quality#11] MAJOR · highlevel.md · §7 Capability map (missing hosting, operations and support) / D2
PROBLEM: D2 chooses hosted, dedicated deployments per design partner, but no capability covers provisioning, upgrades and storage migrations across N deployments, backup and restore, SLOs, incident response or a support channel. C14 promises 'what you saved survives' with no backup or disaster-recovery target, while drafts still sit on local disk and Qdrant has no volume.
SUGGESTED: Add the row "C17 | `operations` | Hosting, reliability and support | Installer, backup, service desk | 🔴 Missing | now → next" with Target: "One-command provisioning of a partner deployment from the product AppHost profile; versioned upgrades with storage migrations and rollback; nightly backups of grain storage, the ledger and vector collections, with a quarterly restore drill (RPO 24 h, RTO 4 h); an SLO of 99.5 % monthly for `/agent` and window reads, with alerts on the per-intent error rate; an incident runbook; a support channel plus an in-app 'Report a problem' that attaches the intent id." Add to the Phase 1 exit: "The first design-partner deployment is provisioned, backed up and restore-tested."

[pm-quality#12] MAJOR · highlevel.md · §12 Decisions / §11 Phase 3 'Commercial readiness' / §13 Regulatory
PROBLEM: Design partners' business data (customer rows) reaches LLM providers from Phase 1 through bounded reads in the model channel. Yet terms, privacy policy and counsel come only in Phase 3, and there is no DPA, sub-processor list, retention policy or account/workspace deletion (C04 covers export and erasure for My Data only). The 'Private individual' persona and consumer-style PR copy pull EU consumer rules (withdrawal rights, CPC principles) into v1, and no decision fixes the launch market or customer type.
SUGGESTED: Add "**D16** | Launch market and customer type | B2B only (business accounts) in v1, EU + US; no consumer sales before Phase 5; the Private-individual persona moves to `later` | —". Add to the Phase 1 exit: "Before the first design partner connects data: a DPA with a sub-processor list (LLM providers, cloud host), a privacy policy, a retention policy (chat, receipts and traces ≤ 30 days unless kept), and workspace/account deletion that also purges vectors and backups." Rename the Phase 3 epic to "*Commercial readiness* (pricing page, payment terms, counsel on Compute)".

[pm-quality#13] MAJOR · highlevel.md · C06 Identity, permissions and approvals / Phase 2 and Phase 3 exits
PROBLEM: The Budget-owner persona ('when my team runs paid tasks') and the self-creator's anxiety 'Who else can see it?' need multi-user accounts, roles and sharing. C06 has principals but no members, roles, invitations or sharing, although the August model had Owner/Admin/Builder/Viewer. The Phase 2 exit says 'auth is on by default', while 'Accounts and login exist' only in Phase 3, so it is unclear what authenticates in Phase 2.
SUGGESTED: Add to the C06 Target: "An account has members with two v1 roles (owner, member). Workspaces are private by default and can be shared with members. Only owners set budgets and approve installs." Replace the Phase 2 bullet with "Principals travel on every call and are checked by a platform filter; the single-owner login is on by default." Replace the Phase 3 bullet with "Accounts with invited members and owner/member roles exist." If teams are out of scope, add the non-goal "One user per account through Phase 3" and move the Budget owner to `later`.

[pm-quality#14] MAJOR · highlevel.md · §7/§9 (missing first-party app portfolio) / D1 / Phase 3
PROBLEM: The cold-start mitigation depends on 'supply from first-party manifests', but the plan never lists which first-party apps ship in which phase. D1 'narrows the proposed five-app release' without saying which of Image Studio, Files, Tables, Notes and Automations survive. Phase 3 names 'Image Studio', but the current product has 'Image Editor'. The first paid capability (BackgroundRemover on product photos) serves an e-commerce job, not the Supabase/RevOps ICP of D1, so Phase 3's approve-then-charge test runs on a job the design partners may not have.
SUGGESTED: Add "### 9a. First-party apps" with the table: | App | Job | Kind | Ships | / Customer tables (J1) | see and refine business data | declarative | Phase 1 / Forms & My Data (J2) | capture and reuse typed details | declarative | Phase 1 / Files | keep documents | module + generated manifest | Phase 2 / Image Editor | edit images | module + generated manifest | Phase 2 / LeadGenerator (J3) | find companies to contact | declarative | Phase 2 / BackgroundRemover | clean product photos | first-party paid operation in Image Editor | Phase 3 / Notes, Tables app | — | not planned (D1) | — |. Append to D1: "The first paid capability must be one the design partners use every week. If the partners are RevOps, make paid company enrichment in LeadGenerator the first charge and BackgroundRemover the second." Replace 'Image Studio' with 'Image Editor' on line 1005.

[pm-quality#15] MAJOR · highlevel.md · §12 D8 Pricing / §5 O4 / §1 promise 3
PROBLEM: D8 prices only tokens, at 20 % over provider list price. D2's dedicated deployment per partner adds fixed hosting cost (silo, storage, Qdrant) that a token margin cannot cover at design-partner volume, and 'gross margin per intent' hides it. There is no packaging: no free-grant size, no workspace fee, no minimum. Promise 3 ('You never pay for our mistakes') and 'platform failures free' are undefined: is a misread request a platform mistake? Hidden calls (the untrusted-content screening model, embeddings) are never said to appear on receipts.
SUGGESTED: Replace D8 with: "A flat published margin over provider list price (start at 20 %), **plus** a monthly workspace fee that covers hosting while deployments are dedicated (set from Phase 2 cost data); a monthly free grant on first-party meters, sized from shadow data. Gross margin is reported per workspace and includes allocated hosting, embeddings, screening calls and free failures. **Our mistake** = any run that ends in a platform error class (runtime, tool contract, validation, timeout, provider 5xx after retries); the whole intent is not charged. Model misunderstandings the user corrects are charged, never above the approved cap. Hidden calls (screening, embeddings) appear as receipt lines." Change the O4 margin indicator to "Gross margin per workspace including hosting ≥ target".

[pm-quality#16] MAJOR · highlevel.md · §1 Press release / §14 FAQ
PROBLEM: The PR's problem paragraph says chat assistants 'answer in text, leave no working tool behind'. The doc's own §13 lists ChatGPT Apps and Claude as competitors, and research I9 documents MCP Apps and OpenAI Apps rendering interactive UI, so the claim undermines credibility. The FAQ lacks the standard PR/FAQ questions: externally support, where to get it or whether there is a free tier, and what happens to my data if I leave; internally competition, market size, cost to build and run, and assumptions.
SUGGESTED: Replace lines 39-42 with: "People who run a business spread their work across a dozen tools. Today's assistants can reach that data and even draw small apps, but what they make stays inside one chat, is gone next week, and nobody can say afterwards exactly which data a request touched or what it cost. Assistant app stores show permissions and prices per app, not per request." Add external FAQ items: "*How do I get help?* ... ", "*Is there a free tier?* ... ", "*Can I take my data and apps with me?* Yes: export of My Data, workspaces and app definitions; deletion also purges indexes and backups." Add internal items: "*Why would an operator pick IntoChat over ChatGPT or Claude with connectors and apps, or Retool/Airtable AI?* Live views that stay in a workspace, per-request receipts and caps, and My Data grants. Evidence still needed: A1 interviews and a side-by-side J1 run on ChatGPT with

[pm-quality#17] MAJOR · highlevel.md · C06 vs C12 / C07 stage 2 / §10 tariffs row
PROBLEM: The same enforcement point is split across two capabilities and two horizons. C06 (Phase 2) has 'an enforcing call filter', while C12 (later) defines the broker as a 'grain-call filter plus gateway'. C07 stage 2 puts 'App-function and storage meters ... in the broker', so the owner's own example, a filesystem tariff for blob storage, cannot land before Phase 4. No phase exit mentions a storage meter. §10 says 'local operations are free', and today's Files app works on local files, so no first-party neuron yet stores user files in blob storage.
SUGGESTED: State ownership in C12: "C06 owns the in-silo call filter (Phase 2), which checks principals and grants and records platform-observed meters for first-party operations. C12 owns only the out-of-process gateway, egress rules and the sandbox." Replace the C07 stage-2 sentence with: "App-function and storage meters are recorded by the platform call filter (C06) for first-party apps and by the broker (C12) for third-party apps." Add to the Phase 3 exit: "Files kept in cloud storage are metered as `storage.gb_month` (daily-peak average) and shown on statements; local files stay free." Name the neuron that stores them in blob storage (Files cloud storage or Drive).

[pm-quality#18] MAJOR · highlevel.md · J3 / D14 LeadGenerator / C09
PROBLEM: J3 promises 'Find new leads for dental clinics in Berlin', but no capability names where leads come from. D14 rules out LinkedIn-derived fields and defers Firmledger. The existing `lookup_company` and `websearch` (Tavily) tools are registered but never offered to the assistant. Contact data of clinic staff is personal data under GDPR, and the C07 price book covers only LLM token classes, not search-provider fees.
SUGGESTED: Replace D14 with: "First-party declarative app in Phase 2, built from the existing `lookup_company` and `websearch` (Tavily) tools and public business registries. It returns company-level fields only (name, address, website, phone, category): no personal contact data and no LinkedIn-derived fields in v1. Search-provider fees are a first-party meter (`search.request`) in the price book. Wrapping Firmledger is deferred until counsel review." Add to the C09 Target: "Web search and company lookup are Connections with meters, reachable from chat."

[pm-quality#19] MAJOR · highlevel.md · §6 Non-goals
PROBLEM: Several non-goals are sequencing conditions ('before X', 'unless a named journey needs them') rather than exclusions. 'Replacing Windows or macOS' is a strawman. Exclusions that follow from the plan's own decisions are missing: self-hosting (D2 is hosted), mobile clients, SSO/SCIM, consumer sales, postpaid invoicing, multi-currency, voice (D13 left it open), and writes to connected systems.
SUGGESTED: Replace the non-goals list with two lists. "**Not in v1 (through Phase 3):** self-hosted or on-prem installs; iOS/Android clients (web and desktop only); SSO, SCIM and enterprise admin; consumer (B2C) accounts; postpaid invoicing and prices in currencies other than USD; voice input; writes to connected systems except through preview → confirm; generated Flutter, Dart or HTML; loading third-party or assistant-written assemblies into the silo; `ValueNeuron` per field; a floating or cashable currency; LLM-judged security or spending decisions; resale of LinkedIn-derived data." "**Sequencing rules** (moved to §11 as phase entry criteria): marketplace only after first-party journeys pass on live models; public publishing only after identity, broker, sandbox and ledger; Telegram only after the Flutter client covers the type catalog." Delete 'Replacing Windows or macOS' and keep its clarificati

[pm-quality#20] MAJOR · current-state.md · Missing section (baseline) / §2 What actually works
PROBLEM: The snapshot never says who uses the product today, where it runs (local Aspire only, or hosted), which clients exist, or what today's values are for the metrics highlevel.md targets. Only span counts are measured. The 365-span figure is the date-card intent, and J1's span count is unmeasured, yet O1 sets '≤ 25 spans for Show me all customers'. §2 rows are not mapped to capability keys, so the heat map cannot be traced back to the snapshot.
SUGGESTED: Add "## 9. Baseline measures" with the table: | Measure | Today | Evidence | / Users | 1 (the owner) | A17 / Where it runs | [local Aspire / hosted: state which] | — / Clients | Flutter [web/desktop: state which] | A20 / J1 live-model pass rate | not measured (scripted model only) | A1, F9 / J2 success | 0/1 (failed on 'date') | B1–B6 / Tokens and cost per intent | not recorded | A16, J1 / Spans per intent | 365 (date card); J1 not measured | D1 / Idle spans per minute | ~800 | D7 correction / Intents with a receipt | 0 % | C03 / Plaintext credential fields | ≥ 3 (TextField password, Salesforce access and refresh tokens) | K2, C12 / Accepted intents per week | not measurable | — |. Add a 'Capability' column (C01–C15) to the §2 table.

[em-feasibility#1] CRITICAL · highlevel.md · §7 Capability map, §11 Phases 2–3, §12 D2
PROBLEM: No capability or epic covers hosting and operations. D2 says 'Hosted. One dedicated deployment per design partner', and the Phase 3 exit requires 3 paying design partners for 4 weeks. Nothing schedules a production deployment, key management, backups or restore, a persistent telemetry backend, or partner onboarding. Today's runtime cannot be hosted as it is: the behavior worker is Windows-only and single-host, the SDK is a pinned RC, Qdrant has no data volume, and the planned tail sampling needs an OTel Collector that the dev Aspire dashboard does not provide.
SUGGESTED: Add this row to the §7 table: `| C16 | \`operations\` | Hosting, operations and data safety | System administration + backup | 🔴 Missing (local Aspire only) | next |`. Add a §8 subsection: "### C16 Hosting, operations and data safety (`operations`)\n**Target.** A hosted product deployment (Linux containers, product profile only) per design partner; managed identity; keys in Key Vault; daily grain-storage, ledger and Qdrant backups with a tested restore; telemetry exported to a persistent backend through an OTel Collector with tail sampling; the Windows developer-tier executor disabled in hosted deployments; the SDK moved to the first .NET 11 GA release.\n**Today — Missing.** Local Aspire only (E7, E15, E19)." Add a Phase 2 candidate epic *Hosted deployment & operations* and this Phase 2 exit criterion: "- One hosted deployment passes a restore drill and runs the J1/J2 golden prompts; des

[em-feasibility#2] CRITICAL · highlevel.md · §6 Principle 6; §8 C06 Target, C12 Target; Phase 2 exit
PROBLEM: The plan enforces permissions with 'principals carried on every neuron call and checked by a platform filter', which in Orleans means values in RequestContext. RequestContext is set by the caller, and the Orleans docs say authenticating that value is the caller's responsibility. Behavior workers stay in the product as the developer tier until Phase 4 or 5, and each one gets a full Orleans gateway connection. Any of them can therefore set any principal or call IAgent directly, with no metering. RequestContext also propagates automatically to downstream grains, so an app's neuron that calls another neuron carries the user's principal. That is a confused-deputy path. Together these defeat 'The platform enforces; the model proposes' and the Phase 3 exit '0 paid calls without an allowance'.
SUGGESTED: Add to C06 Target: "- The caller context is stamped only at trusted edges: authenticated HTTP endpoints, the platform app proxy and the scheduler. It is re-stamped, never inherited, whenever a call enters or leaves an app. The proxy sets `app` to the callee and narrows the grant set to that app's grants. Any process holding an Orleans gateway connection is trusted platform code by definition." Add to C11/C12 Target: "- From Phase 2, LLM-authored code never holds a gateway connection. Behavior workers reach neurons only through the broker gateway, the same path `process` apps will use, or run only in the developer profile." Add this Phase 2 exit criterion: "- A test proves that a behavior worker in the product profile cannot open an Orleans gateway connection, and that a principal set in RequestContext by any non-edge caller is rejected."

[em-feasibility#3] CRITICAL · highlevel.md · §9 J2 (Phase 1); §11 Phase 1 exit
PROBLEM: J2 is a Phase 1 journey, yet its acceptance text says 'IntoChat asks "Keep date of birth in My Data? Apps need your OK to read it"'. That needs grants keyed by (app, semantic type, mode). Grants are still listed as missing in C06, apps with identities don't exist until app.json v1 in Phase 2, and principals also arrive in Phase 2. No Phase 1 exit criterion covers grants. The vault's owner key is also undefined, because Phase 1 has no principal, only a workspace hash of the single Basic-auth 'owner'. Research K raises this as an open question: is personal data per person or per workspace?
SUGGESTED: Replace the J2 lines 849-850 with: "When I submit it\nThen IntoChat asks \"Keep date of birth in My Data?\" and, if I agree, stores it in my vault\nAnd no app can read it yet; the card shows \"Saved to My Data\"". Append to J5 (Phase 2): "When Customer intake first needs my date of birth\nThen IntoChat asks \"Allow Customer intake to read date of birth? Once / Always\"\nAnd the grant appears in My Data with Revoke". Add to §12: "| D16 | My Data ownership key | In Phase 1 the vault is keyed by the deployment's single owner id. When accounts ship it is re-keyed to the account principal by a scripted, tested migration. | — |"

[em-feasibility#4] CRITICAL · highlevel.md · §11 Phase 2 and Phase 3 exits; §9 J3; §8 C07 stage 5 and C12
PROBLEM: The identity and spending prerequisites are in the wrong order. The Phase 2 exit says 'Principals travel on every call … auth is on by default', and the principal set includes an account, but 'Accounts and login exist' is a Phase 3 exit criterion. J3 (Phase 2) shows 'the estimated Compute and a monthly cap', but caps and allowances only arrive in Phase 3. The enforcement point also shows up three times: a platform filter in Phase 2, allowances in Phase 3 and the broker in Phase 4. Unless they are declared as one component, that risks three approval paths, which breaks Principle 2 ('one approval path').
SUGGESTED: Rename the Phase 2 epic *Principals & caller context* to *Accounts, principals & caller context*. Move the criterion "- Accounts and login exist." from the Phase 3 exit to the Phase 2 exit, and keep only the wallet in Phase 3. In J3 replace "the estimated Compute and a monthly cap" with "and the estimated Compute (shown, not charged)". Add under C12 Target: "The broker is one component delivered in three increments. Phase 2: an incoming grain-call filter that checks the stamped caller context and grants. Phase 3: the same filter also checks allowances and caps. Phase 4: the same filter plus an HTTP gateway for remote apps, with checks of data classes against the manifest. No other approval path is built."

[em-feasibility#5] CRITICAL · highlevel.md · §6 Principle 10; §11 Phases 1–2; §12 D15
PROBLEM: The plan's WIP rule contradicts its own schedule. The rule is at most 2 epics in progress, yet Phase 1 schedules 7 candidate epics in 6 weeks and Phase 2 schedules 8 in 8 weeks. At WIP 2 that leaves about 1.7 and 2 weeks per epic, although the research cites '2–3 epics per quarter' as the norm. The plan's own Risk #1 ('breadth against a team of one') predicts this failure. No capacity assumption is stated, even though the binding constraint for one owner plus AI agents is owner review and ratification, not coding throughput. '14-day freeze after each ratification' is also ambiguous: it can be read as a work freeze that would consume Phase 0's 2-week appetite.
SUGGESTED: Replace the Phase 1 heading and epics with two slices. "### Phase 1a — Ask and see · appetite 4 weeks · J1", with epics *One assistant runtime*, *Live tables the assistant can read* and *Receipts with shadow cost*. "### Phase 1b — Keep it safely · appetite 5 weeks · J2", with epics *Typed instant forms* (Semantic types v0 and Instant UI merged) and *Secrets by reference, My Data v0 & workspace scoping*. Split Phase 2 the same way: 2a *App format & Save as app*, *Accounts & principals*, *Persistence & migrations*; 2b *Discovery*, *Customer automations & LeadGenerator*, *Metering & token vault*. Add to §16: "Capacity assumption: one owner reviews at most 2 epics at a time; AI agents implement. Appetites are measured in owner-review weeks." Replace "A 14-day freeze follows each ratification" with "A ratified decision cannot be reopened for 14 days (a decision freeze, not a work freeze)."

[em-feasibility#6] MAJOR · highlevel.md · §8 C14 Target (state tiers); §10 ValueNeuron detail; Phase 0/1/2 exits; §12 D4
PROBLEM: Several problems sit around the journaling tier. (1) The persistence-tier ADR lands at the end of Phase 2, but Phase 1 already builds the append-heavy state that tier is meant for: AgentNeuron history (the whole AgentStorage JSON is rewritten on every save, up to 4,096 messages), per-intent receipts and conversation turns. (2) An unused alpha package stays a hard startup dependency through Phase 1. (3) The 2-day spike has no pass criteria and ignores the GA alternative, Microsoft.Orleans.EventSourcing JournaledGrain with the CustomStorage provider. (4) The case for reopening the 4-day-old 09-19 'no journals' decision is thin, because every named candidate can also be met with bounded snapshots or per-row storage.
SUGGESTED: Add to the Phase 0 exit: "- Orleans Journaling is unwired from the product host (package reference, the 'journal' blob resource, AddAzureBlobJournal). The 09-19 'no journals' decision stays in force." Add to the Phase 1 exit: "- AgentNeuron history is bounded (last N turns plus a summary) and receipts are stored one row per intent outside the workspace blob, so no save rewrites unbounded state." Replace the journaled row in the C14 table with: "| journaled (opt-in, only by superseding ADR) | append-heavy or audit state | Reopened only for a measured requirement that bounded snapshots or per-row storage cannot meet. Options compared: Orleans.Journaling (alpha, behind an adapter), EventSourcing JournaledGrain + CustomStorage (GA), Azure Table rows. Spike pass criteria: activates with registered JSON metadata; the adapter absorbs the IJournaledStateManager→IDurableStateManager rename; repla

[em-feasibility#7] MAJOR · highlevel.md · §8 C02 Target; §9 J2
PROBLEM: Instant UI is to be 'built on the existing AppSurfaceComposer'. That composer creates a separate grain for each widget (Button, TextField, Layout). So a 3-field DOB card costs 4 activations, 4 blobs and 4 HTTP GETs, and multi-field updates are not atomic. The plan explicitly rejects this grain-per-value pattern (non-goal line 240; §10: 'The UI kit already shows this (C17)'). It also routes the password field through TextFieldNeuron, the component that stores and broadcasts passwords in plaintext.
SUGGESTED: Replace the first C02 Target bullet with: "- A declarative `show_form` / `show_view` tool. Its definition is validated against the type and component catalogs and stored as **one Form neuron per window**, holding the typed field list, draft values and an atomic submit in a single state. The client renders the whole form from one read. Forms do not use per-widget UI-kit neurons (TextField, Card…); AppSurfaceComposer is reused only to open and lay out the window. A Secret field holds a `SecretRef`, never a value."

[em-feasibility#8] MAJOR · highlevel.md · §12 Decisions; §16 Definition of Done; Phase 1–2 exits
PROBLEM: The plan changes the shape or identity of persisted state in many places but has no migration policy. The changes are: string kinds become enums; neuron keys become workspace-scoped (grain identities change); tokens move out of SalesforceState into a token vault; IConversation is replaced by AgentNeuron; local-disk stores move to the cluster; encrypted state replaces plaintext blobs; and Qdrant gets a volume. The only precedent is the 09-19 'clean break with fresh storage', which becomes unacceptable once design partners hold real data in Phase 3.
SUGGESTED: Add to §12: "| D17 | Stored-state migration | Until the first design partner is onboarded, a breaking state change may use a clean break (new storage container name), recorded in an ADR. After that, every change to a persisted type or grain key ships with a versioned migration (read-old/write-new or a one-off migrator) and a backup whose restore has been tested. Serialized members change only by adding or removing members with new [Id]s, never by changing a member's type. | Extends the 09-19 clean break |". Add to the DoD: "- any change to persisted state names its migration, or the clean-break ADR that covers it."

[em-feasibility#9] MAJOR · highlevel.md · §8 C14 Today/Target; §12 D2
PROBLEM: The multi-silo gap is not mentioned, although the planned hosted deployment would expose it. Inside a silo, SignalSubscription registers only with the local hub and returns without Watch or lease renewal. An in-silo subscriber, such as the SSE endpoints and coordinators, therefore silently misses signals from grains activated on another silo, and gets no 'reactivated' error either. TokenHandoff and BrowserLogins keep pending logins in process-local dictionaries. C14 mentions only 'at-most-once'. D2 plans a 'shared multi-tenant cluster' later without any gate.
SUGGESTED: Add to C14 Today: "- In-silo subscribers register only with the local signal hub, so they silently miss signals from grains on other silos. Pending OAuth handoffs and browser logins live in process memory. (C-missed, C12)". Add to C14 Target: "- Declared topology constraint: one silo per deployment until signal fan-out works across silos and pending logins move out of process memory. The shared cluster in D2 is gated on a two-silo TestCluster test covering signal delivery and login handoff."

[em-feasibility#10] MAJOR · highlevel.md · §8 C04 Target; §5 O3; §10 ValueNeuron detail
PROBLEM: C04 plans 'Encryption at rest through the existing grain-storage serializer seam' alongside 'Export and erasure (crypto-shredding)'. The serializer is registered per storage provider, so it uses one key for all grains and cannot erase a single owner's data. Azure Storage already encrypts every blob and table at rest, and that cannot be turned off, so a global encrypting serializer adds cost for little gain. O3 also says a seeded secret appears '0 times in grain state', which is impossible when the vault stores it. §10 names 'a vault secret' as the example of a value with its own neuron, while C04 describes one vault neuron holding typed fields.
SUGGESTED: Replace C04 Target bullets 4 and 7 with: "- Envelope encryption inside the vault. Each owner has a data key, and each Credential value has its own key; both are wrapped by a Key Vault (or local DPAPI) key. Values are encrypted before they enter grain state. Erasure means deleting the owner's data key (crypto-shredding). Everything else relies on Azure Storage service-side encryption; there is no global encrypting serializer." In O3 replace "appears 0 times in grain state" with "appears 0 times in plaintext in grain state". In §10 replace "a vault secret is the example" with "a shared counter or an externally billed resource is the example; secrets are encrypted fields of the owner's vault neuron".

[em-feasibility#11] MAJOR · highlevel.md · §8 C07 Target stage 2; §10 per-neuron tariffs row
PROBLEM: The owner's own tariff example, a filesystem that stores files in blob storage and charges for storage, cannot be metered where the plan puts it. Stage 2 says 'App-function and storage meters live in the broker', but a call broker sees calls, not bytes held over time. The plan's own meter `storage.gb_month`, a daily-peak average, needs a periodic sampler over blob storage. No phase meters Files storage at all; today Files reads the host's Downloads folder. Embedding calls, which discovery adds in Phase 2, are not metered either.
SUGGESTED: Replace C07 stage 2 with: "2. **Durable metering.** Three meter sources, each writing idempotent meter events keyed by (intent id, meter id, step): (a) a metering `DelegatingChatClient` and an `IEmbeddingGenerator` decorator on every LLM and embedding call site, recording input, cached, reasoning and output tokens; (b) the broker, for per-call app-function meters; (c) a daily storage sampler that records bytes held per owner and app in blob storage, for `storage.gb_month`. Meter events are reconciled to provider usage." Add to the Phase 3 exit: "- Files stored in workspace blob storage appear as a storage line on the statement."

[em-feasibility#12] MAJOR · highlevel.md · §11 Phase 1 exit ('receipt showing shadow Compute'); §9 J1; §8 C03
PROBLEM: Receipts with shadow cost in Phase 1 depend on things no Phase 1 epic or exit criterion schedules: (a) a price book, which is C07 stage 3 and in no phase list; (b) token capture that includes cached and reasoning tokens, when today only 3 counters exist; (c) an intent id that propagates through tools and traces; and (d) a durable receipt store, because O1 requires 100% of intents to have a receipt and traces are evicted after about 47 minutes. The default assistant model can be Gemma via Ollama, which has no provider price, so shadow cost is undefined for it.
SUGGESTED: Replace the Phase 1 exit line with: "- Every intent has a durable receipt, keyed by intent id and stored outside telemetry, listing model calls, tools, data sources and shadow Compute. Shadow Compute comes from price book v0, a data file priced per concrete model for input, cached, reasoning and output tokens. Local models are priced at 0 and labelled 'local'." Add "price book v0 (data file)" and "intent id propagation" to the scope of the *Receipts with shadow cost* epic.

[em-feasibility#13] MAJOR · highlevel.md · §8 C07 stage 4; §12
PROBLEM: The wallet store is never named. The plan says only 'a boring, auditable store, never prerelease technology'. The research recommendation it builds on reads 'a per-account grain with an append-only journal', which is easily taken to mean Orleans Journaling, the alpha package. The platform today has only Azure Blob and Table, Qdrant, and the customer's Supabase. Orleans transactions add constraints: [Reentrant] grains, UseTransactions on both silo and client, and a transactional state store. Picking the store is a new infrastructure dependency that affects the Phase 3 appetite and hosting, so it needs a decision now.
SUGGESTED: Add to §12: "| D18 | Ledger store | A single-writer `WalletNeuron` per account over an append-only ledger table in a relational database (Azure Database for PostgreSQL through the Aspire integration). The idempotency key (intent id, meter id, step) is a unique constraint. No Orleans Journaling and no Orleans transactions on the charge path; developer earnings are posted asynchronously from settled entries. | — |". In C07 stage 4 replace "a boring, auditable store" with "the relational ledger in D18".

[em-feasibility#14] MAJOR · highlevel.md · §16 Definition of Done
PROBLEM: The single DoD cannot be met in practice. Every work item would need 'the affected live-model golden journey passes', but live-model runs are nondeterministic and cost money. The research and O2 treat them as scored nightly evals (≥ 18/20), not per-item gates. The DoD also requires the canary-secret test and the trace budget, which don't exist until Phase 0 or 1 builds them. And it requires 'aspire build and run healthy' for documentation-only items such as ADRs and CONTEXT.md.
SUGGESTED: Replace the DoD block with: "- **Definition of Done (work item):** aspire build and run healthy; tests green at high severity, including the Aspire integration tests; code review done; deletions listed; persisted-state migration named; status and evidence links updated. Documentation-only items need review and a docs-lint pass only.\n- **Feature adds:** the trace budget and canary-secret test are green, once those gates exist (built in Phase 0 and Phase 1).\n- **Epic/phase adds:** the live-model golden journeys meet their nightly threshold (≥ 18/20); a failure opens a bug but does not reopen closed work items."

[em-feasibility#15] MAJOR · highlevel.md · §11 Phase 0 exit
PROBLEM: 'Token usage is persisted for 100 % of /agent turns' puts new persistence code into ConversationCoordinator/IConversation, which Phase 1 deletes ('AgentNeuron is the only runtime'). That work would be thrown away. The research names the shared IChatClient pipeline as the right choke point, and that capture would survive the runtime switch and also serve C07 metering.
SUGGESTED: Replace the Phase 0 line with: "- Token usage (input, cached, reasoning, output) is captured by a metering `DelegatingChatClient` in the shared chat-client pipeline, which every call site including the agent runner now uses. It is persisted per intent and emitted as GenAI spans. Nothing new is added to `ConversationCoordinator`, which Phase 1 deletes."

[em-feasibility#16] MAJOR · highlevel.md · §5 Outcomes and north star; §8 C03
PROBLEM: The north star, 'weekly accepted intents … first try … result the user kept … no repair prompt … within approved Compute', and the O5 ratios ('re-run within 14 days', 'suggestion acceptance rate') need product event data that no capability produces. Traces are sampled and evicted, and the plan itself forbids relying on telemetry for billing, which applies equally to KPIs. As written, the north star cannot be measured.
SUGGESTED: Add under the north-star paragraph: "**Measurement.** Accepted intents are computed from the durable receipt store (C03), not from traces. Each receipt records: first-try outcome, retries, repair prompts, whether the resulting window or app was kept (not closed in the same session), approved versus actual Compute, and whether the capability was discovered or hard-wired. No separate analytics stack is built before Phase 3." Add to C03 Target: "- Receipts are durable records carrying the north-star fields above."

[em-feasibility#17] MAJOR · highlevel.md · §8 C14 Missing ('A durable Job model'); §11 Phase 2; §9 J3
PROBLEM: 'A durable Job with idempotency for paid work' is listed as missing, but no epic in any phase delivers it. J3 in Phase 2 depends on it: 'do this every morning … no automatic retry of side-effect steps'. Today Timer keeps its schedule in memory. A durable scheduler already exists, because ReminderNeuron uses the Orleans reminder table, but the plan doesn't say to reuse it.
SUGGESTED: Add to the scope of the Phase 2 epic *Customer automations*: "Schedules run on Orleans reminders through the existing ReminderNeuron. Each run is a durable Job record with idempotency key (automation id, scheduled time), a Compute budget and a failure policy (no automatic retry of paid or side-effect steps). Timer's in-memory schedule is deleted." Add to the Phase 2 exit: "- A scheduled automation survives a host restart and never runs a paid step twice for the same scheduled time."

[em-feasibility#18] MAJOR · highlevel.md · §11 Phase 2 exit (Discovery); §8 C08
PROBLEM: The discovery gate ('top-5 recall ≥ 0.9 and negative precision ≥ 0.9') is set in Phase 2, when the catalog holds only a handful of first-party manifests. The research says tool selection degrades only beyond 30–50 tools, and each turn already attaches at most 8. At this catalog size a top-5 recall gate is trivially met and proves nothing, while the vector infrastructure has already been built and deleted twice. The gate needs a defined corpus. The owner's 'memory of neurons' (workspace-instance search) still has value at small N and should not be deferred.
SUGGESTED: Replace the Phase 2 discovery criterion with: "- Discovery is measured on a golden set of ≥ 60 prompts (direct, indirect, negative) against a catalog of ≥ 50 operations, including ≥ 20 synthetic distractor manifests, with top-5 recall ≥ 0.9 and negative precision ≥ 0.9. Vector search enters the turn path only when the candidate tool set would exceed 8. Workspace-instance search ('my customers table') ships regardless of catalog size. Discovery never blocks a turn and falls back to keyword search when embeddings are down."

[em-feasibility#19] MAJOR · highlevel.md · §12 Decisions, 'Reverses' column (D1, D3); §8 C11
PROBLEM: Principle 10 requires a superseding ADR for every reversal, but several reversals are missing from the 'Reverses' column: (a) C11 moves the Behaviors console behind developer mode, which reverses the ratified 09-22 Behaviors UI spec and the Behaviors local app shipped in commit ae8b55a94; (b) removing the automatic 3× retry changes the 09-22 programmable-behaviors lifecycle; (c) D1's 'Supabase first' replaces the August strategy's Salesforce-first wedge. Leaving these unlisted invites the re-litigation that Risk #3 warns about.
SUGGESTED: Change the D3 'Reverses' cell to: "Narrows the 09-22 programmable-behaviors default path; moves the 09-22 Behaviors manager UI (ae8b55a94) behind developer mode; replaces the automatic 3× retry with a policy that never retries paid or side-effect steps". Change the D1 'Reverses' cell to: "Narrows the proposed five-app release; replaces the August strategy's Salesforce-first wedge with Supabase-first (Salesforce once reachable); keeps the ratified 09-20 first scenario and 09-21 local apps".

[readability#1] CRITICAL · highlevel.md · Front-matter bullets (Evidence legend, L21-24) and every parenthetical reference
PROBLEM: Evidence references use the same letters and the same '(X9)' notation as the product's own IDs, and none of them are links. Research C1-C24 collide with capabilities C01-C15 (only the zero-padding differs). Research J1-J22 collide with journeys J1-J6, and research D1-D20 collide with decisions D1-D15. Capability references in §9 also use parentheses. The owner cannot tell whether '(C15)' means the dev-kit capability or the 'no caller identity' finding.
SUGGESTED: Replace the Evidence bullet (L21-24) with:

- **Evidence.** A reference written `(ev A8)` points to finding A8 in a research note. `ev` marks evidence so it never reads as a product ID: C01–C15 are capabilities, J1–J6 journeys, D1–D15 decisions.

| Letter | Research note |
|---|---|
| A | [Current flow](research/2026-09-23-A-current-flow.md) |
| B | [Behaviors](research/2026-09-23-B-behaviors.md) |
| C | [SDK, types and kernel](research/2026-09-23-C-sdk-types-kernel.md) |
| D | [Trace noise](research/2026-09-23-D-trace-noise.md) |
| E | [Modules, discovery, memory](research/2026-09-23-E-modules-discovery-memory.md) |
| F | [Existing docs and strategy](research/2026-09-23-F-existing-docs-strategy.md) |
| G | [IAW prototype](research/2026-09-23-G-iaw-prototype.md) |
| H | [Projects survey and BDD](research/2026-09-23-H-projects-survey-bdd.md) |
| I | [Marketplace and app format](research/2

[readability#2] CRITICAL · highlevel.md · §10 Detail: ValueNeuron (L924) and §12 D4 'Reverses' cell (L1055)
PROBLEM: The plan says it 'partially reverses the 2026-09-19 "no journals" decision'. The 09-19 journals deletion removed DigitalBrain's own Journal concept, and §15 of this same document retires that concept. What the plan actually reverses is the 09-19 rule that modules use IPersistentState directly after the DurableGrain-based Neuron was removed. As written, the owner would think he is re-adopting the concept the glossary tells him is retired.
SUGGESTED: Replace L924 with: '- This partially reverses the 2026-09-19 decision that modules use Orleans `IPersistentState<TState>` directly instead of the old `DurableGrain`-based Neuron ([spec](../superpowers/specs/2026-09-19-framework-simplification-design.md), "State and failure semantics"). It does **not** bring back the deleted DigitalBrain Journals. It is recorded as an ADR.'

Replace the D4 'Reverses' cell with: 'Partially reverses 09-19 "modules use `IPersistentState` directly" (the `DurableGrain`-based Neuron was removed). Does not restore DigitalBrain Journals.'

[readability#3] MAJOR · highlevel.md · Top of document (after L27) and section order
PROBLEM: The document is 1,226 lines and has no table of contents or reading path. Its only in-document links point to §12 and §15. The two sections written for the owner, the answers to his ideas (§10) and the decisions he must make (§12), start at lines 896 and 1045. A reader who stops after the press release, the OS model and the persona tables never reaches either one.
SUGGESTED: Insert after the '---' at L29:

> **Read this first (5 minutes)**
> - **What IntoChat is:** an assistant that turns plain requests into live windows and reusable apps. It runs on an app platform (the "OS") with a marketplace and a pay-per-use currency, Compute.
> - **Where it is today:** one journey works ("Show me all customers"). Any UI request goes through a C# compile-and-deploy pipeline, which is why the date card failed. There are no user accounts, costs, app format or discovery. See [current-state.md](current-state.md).
> - **The plan:** Phase 0 (2 weeks) deletes dead code and cleans traces. Phase 1 (6 weeks) makes J1 and J2 work with typed data, instant forms, My Data and receipts. Phase 2 adds apps, discovery and metering, Phase 3 adds money, Phase 4 invited developers, and Phase 5 the open marketplace.
> - **What I need from you:** ratify D1–D4 in [§12](#12-decisions-needed-fro

[readability#4] MAJOR · highlevel.md · §10 Answers to the owner's ideas
PROBLEM: This section omits five things the owner raised: (1) the unclear 'Show me all customers' flow, (2) the failed date card, (3) 'a lot of trash' in general, not only in traces, (4) his example of LeadGenerator 'from the marketplace', which the plan quietly changes to a first-party app (D14), and (5) how an app tells what it does ('neuron interface?'). The answers exist elsewhere in the document, but not where he will look for them. The table cells are also 60-110 words each (the BDD row especially), which is hard to read as a markdown table.
SUGGESTED: Add these rows to the §10 table:
| "It's not clear how 'Show me all customers' works" | **answered** | current-state.md §3 walks the flow step by step. Phase 0 makes it one readable trace. Phase 1 lets the assistant read and refine the same window (J1). |
| "Draw me a card … date of birth" failed on `"date"` | **fixed in Phase 1** | Three causes, three fixes (current-state §4): a type catalog so `"date"` is either valid or rejected up front (C05), a declarative form tool so nothing compiles (C02), and a real path into the workspace. Target: J2 in ≤ 10 s. |
| "A lot of trash" | **Phase 0** | ~3,100 lines of dead Dart, 15 dead C# files, the unused agent runtime, the demo modules and the stale glossary are deleted first. Every later epic names what it deletes. |
| LeadGenerator from the marketplace | **adopt, modified** | It ships first as a first-party declarative app (Phase 2, D14), becau

[readability#5] MAJOR · highlevel.md · §3 OS model (L96-98), §10 first row (L900), §12 D12
PROBLEM: The owner's headline vision is 'IntoChat as a new operating system'. The plan demotes 'OS' to architecture and developer messaging ('not the customer promise'; 'Customers hear five nouns, not "OS"'), but no decision in §12 asks him to ratify that. A reframing of the core vision should be an explicit decision, not a sentence inside §3.
SUGGESTED: Append to the D12 Recommendation cell: '; "operating system" is the architecture and the pitch to developers and creators, not the customer message (§3). Customers hear Workspace, Assistant, Apps, My Data, Compute.' Also change D12 to bold, so it joins D1–D4 as a decision to take first.

[readability#6] MAJOR · highlevel.md · §7 Capability map, paragraph after table (L273-276)
PROBLEM: The count is wrong, and the grouping it uses to justify the build order overlaps with itself. The table has 9 Missing and 5 Partial capabilities, which makes 14 of 15, not 12. The text then calls C01-C05 'experience' and C04-C07 'foundations', so C04 and C05 fall into both groups.
SUGGESTED: Replace L273-276 with: 'Fourteen of fifteen capabilities are Missing (9) or Partial (5); only the runtime (C14) is Usable. That is the honest shape of "we miss a lot". It also explains the order: the customer-facing capabilities (C01–C03) fail today on problems that are cheap to fix, the data and trust foundations (C04–C07) come next, and the ecosystem (C10–C13) can't start until those foundations exist.'

[readability#7] MAJOR · highlevel.md · Status-words legend (L25-27) and §7 table
PROBLEM: The horizons now, next and later are never tied to phases, yet §16 uses them to decide which epics get features and work items. The legend lists 'Solid', but no capability uses it, and the emoji colours are never explained. Some statuses fall outside the defined vocabulary ('Missing for users, poor for operators', 'now (shadow) → next'). The owner has to read five phase headings to find out when a capability is worked on.
SUGGESTED: Replace the Status-words bullet with: '- **Status words.** Maturity: 🔴 Missing (nothing usable) · 🟡 Partial (one path works, or only for developers) · 🟢 Usable (works end to end for one owner on one host) · ✅ Solid (works for many users). Horizon: `now` = Phase 0–1, `next` = Phase 2–3, `later` = Phase 4–5. Items marked **proposed** still need the owner's ratification.'

Add a 'Phases' column to §7: C01 P0–1 · C02 P0–1 · C03 P0–1, 3 · C04 P1–2 · C05 P1 · C06 P1–3 · C07 P1–4 · C08 P2, 4 · C09 P1–2 · C10 P2–5 · C11 P2–3 · C12 P4–5 · C13 P4–5 · C14 P0, 2 · C15 P0, 2, 4. Change C03's Today cell to '🔴 Missing'.

[readability#8] MAJOR · highlevel.md · §8 Capabilities (L278-826, ~45% of the file)
PROBLEM: Each capability has a 'Target' list followed by a 'Missing' list that mostly restates it word for word. Several Missing lists simply say 'All of the stages above' or 'Everything above'. That doubles the longest chapter without adding information, and the owner cannot see at a glance which target items already exist.
SUGGESTED: Merge Target and Missing into one marked list, and put the status in the heading. Example for C02:

### C02 Workspace, windows and instant UI (`workspace-ui`) — 🟡 Partial · now
**What it is.** (unchanged)
**Today.** Live tables work. "Draw a card" became a 9-step C# deploy that failed on `"date"` and would have been invisible anyway. 29 UI kinds exist, but app windows render only 9. (ev A5, A8, A19, A20, B5, D3)
**Target** (⬜ missing · 🟡 partly there):
- ⬜ Declarative `show_form` / `show_view` tool, validated against the type and component catalogs, built on the existing `AppSurfaceComposer`.
- ⬜ Form primitive: draft state, atomic submit, server-side validation.
- ⬜ Date and masked-secret inputs, with server and renderer kinds in parity.
- 🟡 Renderer registry covering every catalog kind, with declared fallbacks (9 of 29 today).
- ⬜ One window reference: "this window shows neuron X".
- ⬜

[readability#9] MAJOR · highlevel.md · §3 The OS model: diagram (L100-126) and 14-row table (L128-143)
PROBLEM: The diagram stacks Experience → Platform services → Ecosystem (apps, marketplace) → Foundation. That puts apps underneath the services they consume, puts the broker (the syscall gate between apps and kernel) inside 'Ecosystem', and places the SDK next to the kernel. The arrows have no stated meaning. The 14-row table below it repeats §7's 'OS analogy' column, and its 'Today' cells are cryptic ('0.13 % signal (D2)', 'strings (C8)'). Those cells also carry most of the ID collisions.
SUGGESTED: Replace the diagram with the version below (valid Mermaid). Delete the L128-143 table, because §7 already carries the OS analogy and today's status. Keep the six 'metaphor breaks'.

```mermaid
flowchart TB
    subgraph People["Customer-facing"]
        AS["Assistant (the shell)"]
        WS["Workspace: windows and instant UI"]
        RC["Receipts"]
        MD["My Data"]
    end
    subgraph Apps["Apps"]
        AP["Apps: format, install, versions"]
        AU["Automations and code handlers"]
        MK["Marketplace"]
    end
    subgraph Services["Platform services"]
        IS["Broker: checks every app call"]
        TY["Semantic types"]
        ID["Identity, permissions, approvals"]
        CP["Compute: meters, prices, wallet"]
        DS["Discovery and system memory"]
        CN["Connections"]
    end
    RT["Neuron runtime (kernel, Orleans)"]
    DK["Creator and developer kit"]
    

[readability#10] MAJOR · highlevel.md · §11 Phases (L927-1043)
PROBLEM: There is no one-screen summary of the roadmap. The owner has to read 115 lines to learn the sequence, the total time and what he will be able to do after each phase. The phase headings pack in appetite, an ambiguous 'single owner' and capability code lists, which produces unreadable anchors. Phase 0 prefixes the list with 'capabilities' and the other phases don't.
SUGGESTED: Insert at the top of §11:

| Phase | Goal | Journeys | Appetite | At the end you can… |
|---|---|---|---|---|
| 0 Clear the desk | Delete dead code and demos; one trace per request | J1 (narration only) | 2 wks | read one clean trace for "Show me all customers" |
| 1 Ask, see, keep, safely | Typed values, instant forms, My Data, receipts with shadow cost | J1, J2 | 6 wks | get the date-of-birth card in ≤ 10 s and refine a table by asking |
| 2 Make it yours | App format, Save as app, discovery, metering, principals | J3, J5 | 8 wks | save a result as an app and install LeadGenerator |
| 3 Pay as you go | Accounts, wallet, allowances, first paid app | J4 | 8 wks | pay for BackgroundRemover within a cap you approved |
| 4 Invited developers | Broker, remote apps, certification, payouts | J6 | 10 wks | use an app a third party published |
| 5 Open marketplace | Creators publish; sandboxed c

[readability#11] MAJOR · highlevel.md · §6 Principle 10 and §11 candidate epics
PROBLEM: 'At most 2 epics in progress at once' sits next to 7 candidate epics in a 6-week Phase 1 and 8 in an 8-week Phase 2. That means every epic, including 'Discovery & system memory' and 'App format v1 & Save as app', must finish in about 1.7-2 weeks. The document never states this arithmetic, so the owner cannot tell whether the plan is realistic or what gets cut first.
SUGGESTED: Add under the §11 intro: 'With WIP ≤ 2, a phase of N weeks holds about N two-week epics. Candidate epics below are listed in priority order. When the appetite runs out, the bottom of the list moves to the next phase.' Then reorder each list by priority, for example Phase 1: One assistant runtime · Semantic types v0 · Instant UI · Receipts with shadow cost · Workspace scoping · Secrets by reference & My Data v0 · Live tables the assistant can read.

[readability#12] MAJOR · highlevel.md · §15 Glossary
PROBLEM: The glossary leaves out the developer terms the owner asked about: grain versus durable grain, module and behavior. It also leaves out the planning and engineering jargon the document leans on. §2 sends readers to §15 for these words, but §15 defines only Neuron and Signal among the engine terms.
SUGGESTED: Add to Developer vocabulary:
- **Grain**: Orleans' unit of state and work, addressed by a key. Every neuron is a grain.
- **Durable grain** (`DurableGrain`): an Orleans Journaling grain that appends every change to a log. It is prerelease and unused today, and it is the opt-in 'journaled' tier here (C14).
- **Module**: a compile-time package of neurons plus configuration, added with `WithModule<T>()`. Today it is the nearest thing to an app.
- **Behavior** (today): a C# program that is drafted, checked, sealed and run as its own process. It becomes the developer 'code handler' tier.

Add a new block, **Planning terms**:
- **Appetite**: the time we agree to spend; when it runs out, cut scope, don't extend.
- **Golden prompts**: fixed real requests run against a live model; their pass rate is a release gate.
- **Shadow cost**: Compute computed and shown on the receipt but not charged.
- **

[readability#13] MAJOR · highlevel.md · §8 C14 tier table (L762-768) and §10 Detail (L911-925)
PROBLEM: The owner asked in Orleans terms: 'grain vs durable grain', and whether a neuron can be a DurableGrain. The answer uses new words (stateless, snapshot, journaled) and never maps them to Grain, IPersistentState and DurableGrain. 'Journaled' also clashes with the 'Journal' concept that §15 retires. Separately, the bullet 'Rename away from `Text`' contradicts C05, whose v0 catalog includes a Text type. The collision is really about a grain type named `text` (research C16).
SUGGESTED: Replace the C14 tier table with:
| Tier | Orleans base | Stores | For | Today |
|---|---|---|---|---|
| stateless | `Grain` (via `Neuron`) | nothing | provider facades: the 17 LLM grains, Supabase | in use |
| snapshot | `Grain` + `IPersistentState<T>` (via `Neuron<TState>`) | whole state as one blob per write | widgets, connection records, the My Data vault | in use, 42 places |
| journaled (opt-in) | `DurableGrain` (Orleans Journaling, alpha) | each change appended to a log | vault audit, conversation turns, receipts | wired, unused; only after a 2-day spike |

Add under it: 'Your `ValueNeuron : DurableGrain` idea is the journaled tier. "Journaled" means Orleans Journaling, not the retired DigitalBrain Journal.'

Replace L925 with: '- If a value ever gets its own grain, don't name the grain type `text`: the UI kit already registers a `text` grain (ev C16). The semantic type can still b

[readability#14] MAJOR · highlevel.md · §12 Decisions needed from the owner
PROBLEM: Each decision shows only the recommendation. It gives no alternative, no cost of choosing differently and no place for the owner to record his answer, so ratifying means taking it or leaving it. The 'Reverses' column cites prior decisions by date only ('09-20 first scenario', '09-21 local apps', '08-10 …') with no links. D14 mentions 'Firmledger' without explaining what it is.
SUGGESTED: For D1–D4, add columns '| Main alternative | If you pick it | Your call |':
- D1: 'Marketplace-first: recruit developers now' | 'Phases 3–4 run before identity, broker or ledger exist (§13 risk 1)' | ☐ accept ☐ change
- D2: 'Stay single-owner local install until Phase 3' | 'No design partners before Phase 3; C06 slips' | ☐ ☐
- D3: 'Keep C# behaviors as the default and add enums only' | '"date" becomes a compile error, but every card still needs a ~19 s check plus a deploy; the J2 ≤ 10 s target is dropped' | ☐ ☐
- D4: '`ValueNeuron<T>` on `DurableGrain` per value (your proposal)' | 'One activation and blob per value, N+1 reads, no atomic multi-field save, dependence on alpha Journaling' | ☐ ☐

Add under the table: 'Prior decisions cited: 09-19 = [framework simplification](../superpowers/specs/2026-09-19-framework-simplification-design.md) · 09-20 = [code-first composition and agent data](

[readability#15] MAJOR · highlevel.md · §1 Press release (L31-61)
PROBLEM: The first thing the owner reads describes date pickers, receipts, My Data, Save as app, developer apps and Compute billing in the present tense. None of these exist yet. The owner's complaint is that he can't tell how the product works, and this opening blurs today with the target. The working-backwards convention is never named.
SUGGESTED: Insert directly under '## 1. Press release': '*Written as if the whole plan had shipped (Amazon's "working backwards" format), to fix the target before the work starts. Almost none of this works today. For today, see [current-state.md](current-state.md).*'

[readability#16] MAJOR · highlevel.md · §9 J1 (L833-843)
PROBLEM: The document has no picture of how a request will flow once the plan is done. The only diagram is the abstract loop in §2. The owner's first question was how 'Show me all customers' works. current-state.md shows today's flow, but highlevel.md has no matching 'after' view, so the change is hard to see.
SUGGESTED: Add under J1's Gherkin (valid Mermaid):

```mermaid
sequenceDiagram
    actor You
    participant A as Assistant
    participant T as Customers window (live table)
    participant DB as Supabase (read-only)
    You->>A: Show me all customers
    A->>DB: read-only SELECT, checked first
    A->>T: open window
    T-->>You: first 25 rows
    You->>A: only London
    A->>T: refine the same window
    T->>DB: paged read-only query
    T-->>You: filtered rows, same window
    You->>A: how many?
    A->>T: bounded read (count)
    T-->>A: 312
    A-->>You: 312 in London
    A-->>You: Receipt · Supabase read-only · 2 model calls · 0.4 Compute ($0.004), not charged
```

Follow it with one line: 'Compare with today: [current-state.md §3](current-state.md#3-show-me-all-customers-step-by-step).'

[readability#17] MAJOR · current-state.md · §3 'Show me all customers' flowchart (L41-55)
PROBLEM: The flowchart is written in code terms: AgentTurnRunner, sha256(owner, workspaceId), LIMIT 0 describe, IWorkspace.Open, optimistic retries, SSE. It doesn't show which box runs in the Flutter app, which in the server and which in Supabase. The owner said the flow isn't clear, and this diagram repeats the code instead of explaining it.
SUGGESTED: Replace with (valid Mermaid), and move the code names into one line under it: 'Code names: AgentTurnRunner · supabase_schema · show_supabase_query_table · SupabaseQueryGuard + LIMIT 0 describe · IWorkspace.Open (≤ 8 retries) · GET /workspaces/{id}/tables/{tableId}.'

```mermaid
flowchart TD
    subgraph App["Flutter app (what you see)"]
        U["You type 'Show me all customers'"]
        T["Customers window, 25 rows per page"]
    end
    subgraph Server["IntoChat server"]
        R["Assistant turn: same prompt and 16 tools every time"]
        SC["Tool: read the database schema"]
        Q["Tool: show a query as a table<br/>(model writes a title and a SELECT)"]
        V{"Allowed and showable?<br/>read-only SELECT, 1–32 columns"}
        W["Open the table as a window in your workspace"]
    end
    subgraph DB["Your Supabase database"]
        P["Each page = a fresh read-only query + 

[readability#18] MAJOR · current-state.md · Missing section: what a 'behavior' is
PROBLEM: The owner said behaviors 'might be simplified', yet the snapshot never explains what a behavior is or how one normally runs. It appears only as '14 … C# programs ("behaviors")' in §1, as a failure sequence in §4, and as unexplained terms in §7 (Code draft, Check/Artifact, Behavior program, Behavior description). Without the lifecycle, the owner can't judge the plan's 'developer tier with 3–4 tools' proposal.
SUGGESTED: Insert a new section before §4, titled '## What a "behavior" is today':

A behavior is a small C# program that the assistant (or a developer) writes to make IntoChat do something new. Getting one running takes five hand-offs, each with its own id:

```mermaid
flowchart LR
    D["1. Draft<br/>one C# file + xUnit tests"] --> K["2. Check<br/>compile and run the tests, ~19 s"]
    K --> A["3. Artifact<br/>sealed build"]
    A --> P["4. Program revision<br/>what should run"]
    P --> R["5. Deployment<br/>its own Windows process joins the cluster"]
    R -.->|"crash: 3 automatic retries"| R
```

- The assistant drives this with 14 tools and has to track six ids.
- What is good: sealed builds, versions and rollback, bounded logs (B17).
- What hurts: it is the only way to show any UI. Checks accept the model's own tautological tests. The process can reach every neuron. There is no delete, and i

[business-legal#1] CRITICAL · highlevel.md · §12 D8 Pricing; §5 O4; §13 Other risks – Negative unit economics
PROBLEM: The pricing decision does not cover costs, and the document never checks the arithmetic. A flat 20% markup over list price leaves 16.7% gross margin. Platform failures are free, and O2's own target of 18/20 implies at least 10% failed intents. IntoChat is merchant of record, so it pays card and tax fees. D2 gives each design partner a dedicated deployment. Together these leave about 3% contribution before any hosting cost. Neither the 20% nor the 10% take rate traces to the research: J20 says the margin 'can be zero like Hugging Face', and I16 benchmarks are 0–15%. O4 measures only 'gross margin per intent', which cannot show fixed per-partner costs.
SUGGESTED: Replace the D8 row with: '| D8 | Pricing | Prices are set from a margin target, not a fixed markup. Target: contribution margin per workspace-month ≥ an explicit threshold (proposed 50–60 %) after absorbed failures, payment and tax fees, and hosting. Ways to reach it, chosen from Phase 1–2 shadow data plus paid design-partner pilots: (A) per-model markup on tokens; (B) a per-accepted-intent platform meter, which is still pay-as-you-go; (C) a workspace plan with included Compute plus pay-as-you-go overage (the GitHub and Cursor pattern). A 20 % markup on provider list price is a floor for pass-through, never the whole margin. Take rate: see D8b. | — |'. Add to O4: 'Contribution margin per workspace-month (revenue − provider cost − absorbed failures − payment and tax fees − hosting) ≥ the D8 target.' Add to §13 'Negative unit economics': 'Check: +20 % over list, ~10 % free failures and ~5 

[business-legal#2] CRITICAL · highlevel.md · §14 FAQ 'Why peg Compute to money?'; §12 D7; C07 Target stages 4 and 6
PROBLEM: The two precedents used to justify a prepaid, non-expiring wallet are not prepaid wallets. Anthropic's CCU is billed in arrears and has no balance at all. GitHub AI Credits are a monthly allotment included in a subscription and forfeited at month end. Most of the plan's regulatory exposure comes from prepaid stored value (PSD2/PSR, voucher VAT, CPC principles, the Stripe restricted list, stolen-card fraud), and the documents never consider a postpaid unit of account. That option is the actual precedent and fits 3 B2B design partners.
SUGGESTED: Replace the FAQ answer with: '*Why peg Compute to money?* So a price is always a price. Anthropic's CCU ($0.01, billed in arrears, with no CCU balance) and GitHub AI Credits ($0.01, a monthly allotment included in the plan and forfeited at month end) both peg a unit to money. Neither is a non-expiring prepaid wallet. We start with the CCU shape: Compute is a unit of account billed monthly in arrears to business customers. A prepaid balance comes only when self-serve or consumer sign-up needs one.' In D7 replace 'purchased Compute is non-expiring...; web/desktop top-ups' with: 'Phase 3: Compute is a unit of account, invoiced monthly in arrears (invoice or card on file), under platform-enforced spend limits. A prepaid wallet (grants, holds, top-ups) is a separate decision, taken before self-serve sign-up opens.' In C07 rename stage 4 to 'Spend limits and holds against a limit, plus monthly

[business-legal#3] MAJOR · highlevel.md · §1 Press release; §2 Three promises; §6 Principle 8; §14 FAQ 'What will this cost?'; D8
PROBLEM: 'You never pay for our mistakes' and 'If IntoChat fails, you aren't charged' are unbounded promises with no failure taxonomy. The owner's own reference flow contains model retries (validate 1–32 columns, retry). Nothing says whether those retries are charged, or whether provider outages, a cap reached mid-run, user cancellation or a third-party app failure count as 'our mistake'. A marketing claim that terms cannot define is a misleading-practice risk, and absorbed costs are unbudgeted.
SUGGESTED: Press release: replace 'You never pay for our mistakes.' with 'If a run fails because of an IntoChat error, you aren't charged for the failed steps, and the receipt shows which steps were charged and why.' Add to Principle 8: 'A published failure policy puts every step that doesn't finish into one of these classes: platform fault (not charged); provider outage (not charged, cost absorbed and tracked); model retry inside a run that succeeds (charged, at most N retries); user cancellation, revoked grant or cap reached (completed steps charged); third-party app fault (not charged and not paid to the developer). Absorbed cost counts as cost of goods and is reported per workspace.' Change O4's '0 Compute charged for platform failures' to '0 Compute charged for steps classified as platform fault or provider outage'.

[business-legal#4] MAJOR · highlevel.md · §12 D8 (take rate); C13 Target; J6
PROBLEM: A 0% take rate while IntoChat is merchant of record means IntoChat pays card fees, tax compliance, refunds, chargebacks and support on every third-party sale, so each sale loses money. '10% later' has no trigger or rationale. The document also never defines what IntoChat actually earns from app usage: the take rate on app meters, or the platform meters an app causes (assistant tokens, storage). It also doesn't say whether apps may use their own model keys.
SUGGESTED: Add a D8b row: '| D8b | Take rate | As merchant of record, IntoChat always keeps a fee that covers its MoR costs (payment processing, tax compliance, refunds and chargebacks; estimate 5–8 %, confirmed from Phase 3 data). During the invited beta the platform fee on top is waived; the MoR fee is not. After the beta: 15 % total, or 0 % platform fee on a developer's first $X of lifetime sales and 15 % above that (Shopify pattern); decided by Phase 4 entry. The fee is computed on app-meter revenue net of VAT. Platform meters an app causes (assistant tokens, storage) are billed to the user at the price book and never shared. Apps that call models through the platform pay for them as platform meters. Apps with their own keys build that cost into their own meters. | — |'

[business-legal#5] MAJOR · highlevel.md · C13 Target; §13 Regulatory exposure; D7
PROBLEM: Merchant of record appears only as a regulatory mitigation. Its obligations and costs are never listed. In the EU, a platform through which electronically supplied services are sold is presumed to be the supplier for VAT anyway, so MoR is close to the default rather than a free choice. The consequences — tax on the full price, supplier agreements and self-billing with developers, disputes, liability for the app's conformity, sanctions screening — shape the take rate, the Phase 4 appetite and the counsel scope, and none of them appears.
SUGGESTED: Add to C13 Target: 'Merchant of record: IntoChat sells every app charge. It collects and remits VAT or sales tax on the full price (in the EU an app marketplace is presumed to be the supplier anyway, Reg. 282/2011 Art. 9a). It handles disputes, refunds and chargebacks, and answers to buyers for the app's conformity. It buys from developers under a supplier agreement, with self-billed invoices where developers are VAT-registered. It screens developers and buyers against sanctions lists. These costs set the minimum take rate (D8b).' Add 'MoR obligations and cost' to the Phase 4 epic *Earnings & payouts*.

[business-legal#6] MAJOR · highlevel.md · §12 Decisions (new row); §13 Regulatory exposure
PROBLEM: The plan never names the selling legal entity, its country, the launch markets or the customer type (business or consumer). Everything in the Compute and payout design depends on these: whether Stripe is available at all, Connect payout regions, VAT/OSS and US sales-tax registration, which payments regime applies (EU PSD2→PSR, UK PSRs 2017, US state money-transmission), and whether consumer law applies. The regulatory list covers only the EU.
SUGGESTED: Add: '| D16 | Selling entity, markets and customer type | Name the contracting entity and its country, and the launch markets (proposed: EU, UK and US business customers). Terms accept business customers only (sole traders included) until Phase 5. This decides: Stripe account availability and Connect payout regions (self-serve cross-border payouts only among the US, CA, UK, EEA and CH), VAT/OSS and US sales-tax registrations, which payments regime applies, and whether consumer law applies. Ratify before any Phase 3 epic starts. | — |'

[business-legal#7] MAJOR · highlevel.md · §4 Who it is for; D1; §1 Press release
PROBLEM: The choice of B2B operator first is asserted, not argued, against the owner's marketplace-for-regular-people ambition. (1) The ICP doesn't match the only working connector. 'SMB owner, ops or RevOps lead with data in Supabase' is an unusual profile: Supabase users are mostly technical founders, and RevOps data lives in Salesforce or HubSpot. Salesforce is unreachable until Phase 2. (2) There is no demand or willingness-to-pay evidence and no named design partners. (3) The document doesn't note that 'Supabase first' departs from the August strategy's Salesforce wedge. (4) It never shows how the B2B wedge leads to a consumer and creator marketplace, which has different trust, law and payment regimes. (5) The press release describes the Phase 4–5 end state ('apps from developers and other users') as if it were the launch. (6) B2B must-haves for paying partners (roles, admin allowlist of apps, invoices with VAT IDs) are absent from the capability map.
SUGGESTED: Add to §4 after the table: '**Why business operators first (proposed).** The owner's anchor journeys (show me all customers, LeadGenerator, Salesforce) are business jobs with a budget owner. Business customers accept per-use pricing, invoices and caps, and they avoid consumer-law, app-store and payout regimes until the platform is ready. Hypothesis: operators who save apps become the first creators, and their saved apps seed the catalog regular people will later browse. We switch to prosumer first if, by the end of Phase 2, fewer than 2 of 3 design partners use the product weekly while prosumer journeys (images, My Data) show stronger pull. ICP for Phases 1–2: technical founders and ops leads at startups whose data is in Supabase; RevOps on Salesforce from Phase 2.' Scope the press release to the Phase 3 launch and move marketplace sentences into a closing 'What comes next' line. Add 'wo

[business-legal#8] MAJOR · highlevel.md · §11 Phase 3 exit criteria; Phase 1–2; §5 north star
PROBLEM: No phase tests willingness to pay or revenue. Phase 3 is framed as '3 design partners paying real money', yet its exit criteria measure only usage. The design-partner commercial arrangement (paid pilot, price, invoicing) is never defined. Shadow data from the owner as user #1 measures cost, not what anyone will pay.
SUGGESTED: Add to Phase 1 exit: 'A design-partner pilot agreement exists (fixed monthly pilot fee plus metered usage, invoiced), and each partner receives a monthly shadow statement: what this month would have cost.' Add to Phase 3 exit: 'Each design partner has paid at least 2 monthly invoices at published prices. At least 2 of 3 renew. Contribution margin per workspace-month meets the D8 target. At least 8 willingness-to-pay interviews are recorded.' Add to §5 companion ratios: 'revenue and contribution per active workspace-month'.

[business-legal#9] MAJOR · highlevel.md · §11 Phase 1/Phase 3; C09; §12 D1
PROBLEM: The counsel gates come too late and are too narrow. Counsel appears only before the first sale (Phase 3) and before third-party charges (Phase 4). But design partners' customer rows, which are personal data, flow to LLM sub-processors as soon as `table_read` ships and partners connect in Phase 1–2. Prompts are also captured in dev telemetry. A data processing agreement, sub-processor list, transfer mechanism, privacy notice and retention rule are needed before any partner connects production data. None of them is scheduled.
SUGGESTED: Add to Phase 1 exit: 'Before any design partner connects production data, these are signed or published: pilot terms; a privacy notice; a data processing agreement with IntoChat as processor; a sub-processor list (LLM providers, hosting) with its transfer mechanism; and a retention rule for prompts, traces and receipts.' Move 'counsel review of data protection' from Commercial readiness into a Phase 1 candidate epic, *Data-protection readiness*.

[business-legal#10] MAJOR · highlevel.md · §12 D14; §9 J3
PROBLEM: The counsel gate in D14 applies only to wrapping Firmledger. The first-party declarative LeadGenerator in Phase 2 does the same processing: it collects details of people at businesses and supports daily outreach. The example journey targets Berlin, where email advertising needs prior consent even between businesses. Source terms (places and map APIs restrict caching; LinkedIn prohibits scraping) and the GDPR Art. 14 notice duty apply whoever builds it.
SUGGESTED: Replace the D14 recommendation with: 'First-party declarative LeadGenerator in Phase 2, only after counsel reviews its data sources and use: source terms (map/places APIs restrict caching and storage; LinkedIn prohibits scraping), the GDPR Art. 14 notice for people it finds, and outreach rules (Germany's UWG §7 requires prior consent for email advertising, B2B included). LeadGenerator finds and shows leads. It does not send outreach in v1. Never deliver LinkedIn-derived fields. Wrapping Firmledger goes through the same review.'

[business-legal#11] MAJOR · highlevel.md · §13 Regulatory exposure; §14 FAQ
PROBLEM: The PSD2 statement is stale and internally inconsistent. (1) PSD2 is being replaced: PSD3 and the Payment Services Regulation reached provisional agreement on 27 Nov 2025, narrow the limited-network exclusion, and are expected to apply around end-2027, which is exactly when Phase 4 would ship. (2) The €1M notification (PSD2 Art. 37(2)) applies to anyone relying on the Art. 3(k) exclusion, not only 'once Compute pays third parties'. (3) Under D7's merchant-of-record design, Compute never pays third parties, so the risk bullet contradicts the design. (4) These are stated as fact, but research J has zero skeptic corrections.
SUGGESTED: Replace the bullet with: '**Regulatory exposure (not yet assessed by counsel).** Counsel decides whether prepaid Compute, sold by IntoChat as merchant of record and accepted only by IntoChat, falls outside EU payment-services law or relies on the limited-network exclusion. Relying on the exclusion requires notification above €1M of transactions in 12 months (PSD2 Art. 37(2)). PSD2 is being replaced by PSD3 and the Payment Services Regulation (provisional agreement 27 Nov 2025, narrower limited-network exclusion, expected to apply around end-2027). A postpaid unit of account for business customers (D7) avoids the question. The UK (PSRs 2017) and the US (state money-transmission rules) need their own check. Research J was not skeptic-reviewed; treat its legal points as questions for counsel.'

[business-legal#12] MAJOR · highlevel.md · §13 Regulatory exposure (VAT); C07; D8
PROBLEM: The document states the VAT risk as 'multi-purpose vouchers'. The research's own source reports that Germany's Federal Fiscal Court (25 June 2025) classified prepaid codes for digital content as single-purpose vouchers, because the place of supply was known at issue. A Compute balance tied to an account in a known country is likely the same, which means VAT is due at top-up. Separately, nothing says whether the price book and the '100 Compute = $1' peg are net of or including tax. If Compute is tax-inclusive for EU consumers, 17–27% VAT takes more than the 20% margin.
SUGGESTED: Replace the VAT clause with: 'VAT treatment of Compute is undecided: a single-purpose voucher (VAT at top-up), a multi-purpose voucher (VAT at spend), or a plain prepayment or postpaid usage. A German Federal Fiscal Court ruling of 25 June 2025 treated country-bound prepaid codes for digital content as single-purpose. A tax adviser decides before Phase 3.' Add to D7: 'The price book and the peg are net of tax. Tax is added at checkout or on the invoice (reverse charge for EU B2B where it applies). Consumers see the tax-inclusive total.'

[business-legal#13] MAJOR · highlevel.md · C07 stage 6 'Top-ups ... through Stripe'; §11 Phase 3 exit
PROBLEM: Stripe top-ups are assumed but never checked. Stripe's restricted-business list covers 'pre-loaded payment cards, gift cards, virtual credits or other products', which need extra due diligence and approval, so selling Compute through Stripe is not guaranteed. Fixed card fees also make small top-ups uneconomic: a $5 top-up loses 8.9% to fees, more than half the 16.7% margin. Yet J15 says top-ups should 'allow arbitrary amounts' so consumers aren't forced to overbuy. The minimum top-up and its justification are undefined.
SUGGESTED: Add to Phase 3 exit: 'Stripe has confirmed in writing that selling Compute is acceptable under its restricted-business policy.' Add to C07 stage 6: 'Minimum top-up set so fixed card fees stay under 5 % of the amount, stated with its reason in the terms. Auto-recharge is opt-in with a cap. Business customers can pay by invoice instead (D7).'

[business-legal#14] MAJOR · highlevel.md · §12 D7; §1 Press release; C07
PROBLEM: 'Always shown in money' never says which money. The peg is to USD, but design partners and consumers in the EU or UK pay in EUR or GBP. Either they are billed in USD (Stripe charges about 2% for currency conversion), or the Compute-to-EUR rate floats daily, which contradicts 'Never float the exchange rate'. The Money type even carries a currency, while Compute does not. The documents also never say whether prices are shown with or without tax.
SUGGESTED: Add to D7: 'Money shown = the currency on the customer's invoice. v1 invoices in USD only, with tax shown separately for business customers. If a local-currency estimate is displayed, it is labelled as an estimate. Per-currency price books (EUR, GBP), and whether Compute amounts stay equal across currencies, are decided before consumer sign-up (Phase 5).'

[business-legal#15] MAJOR · highlevel.md · C07 Target stage 5 'Caps and alerts'; §10 'Per-neuron tariffs'; O4
PROBLEM: The owner's own tariff example (a filesystem charging for blob storage) is a recurring meter that accrues without any intent. Hard caps and a prepaid balance don't say what happens to stored files when a cap or the balance hits zero: stop, go read-only, or delete. O4's 'Actual ≤ approved cap in 100 % of intents' doesn't cover recurring meters, and 'receipts per intent' cannot bill them.
SUGGESTED: Add to C07 Target: 'Recurring meters (storage GB-month, standing automations) accrue without an intent and appear on a monthly statement, not on receipts. When a cap or balance is reached, new paid work stops, but stored data stays readable and exportable for a grace period (proposed 30 days), with notices at 75/90/100 %. Deletion happens only after the grace period and a final notice, and follows the erasure rules in C04.' Add to O4: 'Recurring meters never exceed the monthly cap by more than one day's accrual.'

[business-legal#16] MAJOR · highlevel.md · §6 Non-goals; §12 D7; C07 stage 4
PROBLEM: 'Purchased Compute is never cashable' is absolute and conflicts with obligations the plan itself acknowledges. Consumer withdrawal rights for unused Compute (the CPC principle that the 'right of withdrawal should be respected'), refunds when IntoChat closes an account or discontinues the service, billing errors and chargebacks all mean money returning to the payer. C07 already plans 'refunds as new entries'. The wording should separate 'not redeemable or transferable as value' from 'no refunds ever'.
SUGGESTED: Replace the non-goal with: 'A floating or tradable currency. Compute can't be transferred between customers, gifted or exchanged for cash, except refunds to the original payment method where the law or our terms require them: unused Compute on withdrawal, account closure or service end by IntoChat, and billing errors.' Use the same wording in D7.

[business-legal#17] MAJOR · highlevel.md · §13 Fraud; C07; C13
PROBLEM: The fraud controls leave out the most common abuse vectors for AI platforms. Free-grant farming through multiple accounts turns each free grant into real LLM spend. Card testing hits the top-up endpoint. New accounts have no spend ceiling. The payout holding period has no length, although card disputes can arrive about 120 days later. The research's own mitigations (J18) for new-developer velocity limits and self-dealing are dropped.
SUGGESTED: Replace the fraud bullet's mitigation with: '→ One free grant per verified business or payment method, released only after card-on-file or business verification. New accounts start under a low spend ceiling that rises with payment history. Top-ups are rate-limited, with card-risk scoring and 3-D Secure. Payout holding period ≥ the card dispute window (proposed 120 days). New developers have velocity limits. Earnings from accounts that share a payment method, device or IP with the developer are held for review. Paid calls always go through platform allowances.'
