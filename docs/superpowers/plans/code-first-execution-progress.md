# Execution ledger — code-first-composition and agent-data-window

Plans: 2026-09-20-code-first-composition.md; 2026-09-20-agent-data-window.md.
Approved design: option A; interactive table window in the existing workspace.
Base: 3b419529 on archv2. No push authorized.

Ruling: execute in the clean archv2 checkout — the user explicitly moved work here and deleted the previous worktree — avoids another branch/merge workflow; preserve all existing stashes.
Ruling: use this tracked ledger with native PowerShell commands instead of the skills' bash scratch scripts — Windows environment; same persistent task/test record.
Pre-flight: composition tasks 1–3 share typed module contracts and explicit-member patches; default-value assignments must survive overrides.
Pre-flight: tasks 2–4 share deferred resource materialization; both runtime and client WithReference must freeze once.
Pre-flight: window tasks 1–3 share stable table/window IDs, operation receipts and revisions; never reopen a closed window on duplicate result replay.
Pre-flight: window tasks 3–4 share trusted scope/run/call context; tool factories must be per invocation, not cached request closures.
Pre-flight: window tasks 2/5/6 share current Dart table contract and server-scoped routes; preserve paging/filter types and local geometry.

Composition task 1: in progress.

Composition task 1: complete. RED: missing new interfaces (composition-task1-red.log). GREEN: framework 38/38 (composition-task1-green.log). Ruling: add a generic module-contract base for copy/explicit-member JSON patch mechanics; concrete module contracts still own allowed members and mapping. Baseline framework 32/32.
Composition task 2: complete. RED: missing typed module methods (composition-task2-red.log). GREEN: framework 42/42 (composition-task2-green.log), including deferred Postgres and client-first Web finalization. AI optional endpoint/profile fields are compiled explicitly; generated provider secrets remain Aspire parameter references. Ruling: use a distinct supabase-database resource name and supabase-connection parameter name to avoid collision with the module resource; external connection lookup retains its configured name.
Task 2 additional gates: AI 1/1, Supabase 8/8, Flutter unit 28/28 and Flutter integration 25/25 passed. Composition task 3: core/builders complete; RED composition-task3-red.log; GREEN framework 51/51, external framework 7/7 including optional endpoint projection, Supabase 8/8 including typed local provider. E2E application caller migration follows full graph task 4. Ruling: retain old overloads only until task 5; overrides use a bounded 32 KiB envelope for the eleven-module graph. Ruling: module-owned local service callbacks are retained only in local composition and explicitly rejected at every process boundary; expose Supabase provider contract and query plan/page types to support public typed replacement. Parameter-prefixed private settings are added to Aspire configuration after graph construction, before lazy parameter resolution; no secrets in process arguments.
Task 4 in progress. Full explicit application graph model passes (eleven production modules plus explicit Twitter demo). First full runtime failed on pre-existing Excel/Flutter sheet identity collision; regression RED in composition-alias-red.log then 2/2 model tests GREEN. Ruling: namespace Excel wire/grain identity as excel.spreadsheet while retaining Flutter sheet UI vocabulary. This is a breaking persisted Excel identity change; no existing data was deleted or migrated. Second runtime gate found Salesforce intentionally rejects loopback; add explicit WithLocalMcp limited to loopback, retaining hosted endpoint validation. Ruling: adapters bind their compiled module snapshot, not ambient topology; generated runtime resource values project after public settings. Standalone MCP Program currently has no Orleans client/brain registration; do not revive it just to mirror old wiring (plan allows supported runtime only).
Composition tasks 4 and 5: complete. Full graph health 1/1 and original UiKitTwoWayWebFacts 1/1 passed with real disposable resources. Final pre-window full solution gate: 226/226 passed, 0 skipped (composition-full-suite.log, 2m46s). Excel 3/3 and Salesforce 9/9 passed. No repeat of the previous cleanup failure. Removed central configuration project/interface, full-snapshot E2E input and eager AddModule authoring. Migrated every caller; remaining StartAsync/Modules setters are internal implementation only. Ruling: commit full graph and caller removal together because application E2E now requires the explicit full test deployment. Ordinary AppHost callbacks remain directly readable. Workspace task 1: RED missing contract workspace-task1-red.log; GREEN Flutter unit 34/34 workspace-task1-green.log. Workspace files remain a separate next commit.
Workspace task 1: complete, persisted operation receipts, stale-revision checks, bounded identifiers, workspace isolation and publish-after-persist verified; Flutter Unit 34/34. Starting workspace HTTP/table adaptation.

Agent data task 2: HTTP scoped workspace/table APIs, SSE revisions, JSON scalar adapter and cancellable Dart client implemented. Real database HTTP test passes filtering beyond page one, sort, page, stale/foreign requests and close/reopen (workspace-task2-http-expanded.log). Flutter protocol tests 2/2, Dart core analysis clean. Composition acceptance: full solution 226/226, zero skipped (composition-full-suite.log).

Agent data task 3: durable query-operation journal + idempotent table creation implemented; actual PostgreSQL recovery/close-replay/invalid-query/empty tests pass (workspace-operation-green.log). Supabase suite 9/9. Task 4 in progress: shared per-run AI runner, durable conversation and SSE route; AI suite 4/4 and actual OpenAI-protocol HTTP journey 1/1 (agent-http-first.log). Ruling: add runtime identity to Conversation.Begin so a new runtime can recover abandoned active runs without interrupting concurrent runs in this runtime. Task 5 in progress: remote window projection and scoped table controller wiring; controller/model tests 3/3. Task 6 scripted model fixture introduced early to validate task 4 with the production HTTP adapter.
