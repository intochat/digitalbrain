encryptionsalt: v1:1mTrpGijTVE=:v1:AvipetckTQKaHa+L:NbyRQ4eNUUDLr1Yu7UywG1VqnUlyWg==
config:
  digitalbrain-deploy:imageTag: v4
  digitalbrain-deploy:usePlaceholderImages: "false"
  # Telegram transport secrets — set via: pulumi config set --secret telegramBotToken <value>
  # (no real values committed; transport boots idle when token is absent)
  # digitalbrain-deploy:telegramBotToken: <set-via-pulumi-config-set-secret>
  # digitalbrain-deploy:internalServiceKey: <set-via-pulumi-config-set-secret>
