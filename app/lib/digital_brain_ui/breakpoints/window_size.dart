/// Material 3 window-size classes. Width thresholds are the spec's locked
/// floors: compact < 600 < medium < 840 < expanded < 1200 < large < 1600 ≤ xLarge.
enum WindowSize { compact, medium, expanded, large, xLarge }
