name: digital_brain_sdk_flutter
description: Self-instrumenting perf SDK for the DigitalBrain Flutter client.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.11.0
  flutter: ">=3.41.0"

dependencies:
  flutter:
    sdk: flutter
  uuid: ^4.5.1

dev_dependencies:
  flutter_lints: ^6.0.0
