# DigitalBrain.DevTools

Optional development-only adapters for hosting the official Microsoft Orleans Dashboard and Microsoft Agent Framework DevUI against an owner-bound DigitalBrain client.

The package defaults to Development environments and direct loopback callers. Production requires explicit opt-in and a token. Remote access requires its own explicit opt-in and a token.
