{
  "name": "digitalbrain-docs",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vitepress dev --host 0.0.0.0",
    "prebuild": "node tools/render-specification.mjs",
    "build": "vitepress build",
    "preview": "vitepress preview --host 0.0.0.0",
    "pretest": "node tools/render-specification.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
