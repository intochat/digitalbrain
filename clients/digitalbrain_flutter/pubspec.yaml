name: digitalbrain_flutter
description: >
  DigitalBrain OS pixel host (Designed Windows desktop). Pure Dart projection
  and edge client until Flutter SDK is available in the agent environment.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.12.0

dependencies:
  digitalbrain_wire:
    path: ../digitalbrain_wire
  http: ^1.4.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
