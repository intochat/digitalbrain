name: digitalbrain_wire
description: >
  Dart UI-edge DTOs for src/modules/flutter/DigitalBrain.Modules.Flutter.Http. Golden
  pin lives in tests against src/modules/flutter/DigitalBrain.Modules.Flutter.Contracts/
  flutter-wire-contracts.golden.json (single oracle; no forked copy).
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.12.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
