name: digitalbrain_wire
description: >
  Dart UI-edge DTOs for hosts/DigitalBrain.Ui. Golden pin lives in tests against
  modules/.../flutter-wire-contracts.golden.json (single oracle; no forked copy).
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.12.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
