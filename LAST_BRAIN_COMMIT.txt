commit brain 2026-06-25

Core + Kernel best implementation progress (follow-up)

Changes under brain/:

* DigitalBrain.Core/
  - Distribution/IPackBehavior.cs: PackManifest record + GetManifest() default for typed dispatch contract
  - INeuron.cs: added GetCausalLineageAsync + GetTimelineForCorrelationAsync
  - RestartResource.cs: added IsRollingUpdate, TargetVersion, Strategy for HA rolling updates
* DigitalBrain.Silo/
  - Company/CompanySkillOrchestratorNeuron.cs: kernel update now does pre-checkpoint, uses causal lineage query, richer payload, UiSurface emission
  - SystemNeurons.cs: rolling-aware Restart handler + neuron-driven kernel-rolling UiSurface
  - Neuron.cs: impl of causal queries over journals
  - Foundry/PackAlcEmbodier.cs, Generated dispatch updates for manifest
  - Other: SkillPackSynthesizer, KernelSurfaceDemo updated for manifest
* DigitalBrain.Tests/
  - Foundry/PackAlcEmbodierTests.cs: manifest assertions
  - Features/NeuronCore.feature: kernel self-update rolling Reqnroll scenario

Verification performed after edits:
- dotnet build (AppHost + core projects) clean
- High-severity tests (filters on NeuronCore, Pack, UiSurface, Company, Checkpoint) green
- aspire doctor (MCP) pass
- aspire mcp tools used

Relative paths, no vacuous summaries, self-explanatory names, Context7 for Orleans/Aspire, latest central packages.

Refs: Projects/docs/continuation-core-kernel-best-impl.md

Latest: explicit rolling with drain/verify phases per replica (3), checkpoint+causal per phase, phased UiSurfaces + RfwCards from neuron.
Verifs: build 0 err, tests green, aspire doctor pass, coverage collected.

Packaging step: bumped DigitalBrain.Core to 0.3.0, updated description to reflect manifest, causal, explicit rolling.

[committed e769855] feat(kernel): explicit rolling drain/verify phases + packaging version bump + test scenario update

Followed by verifications: build clean, 39 high-sev tests pass, aspire doctor pass.

Continued: enhanced main kernel dashboard surface emission (richer declarative props for full UI from neuron: haReplicas, panels, status; kind=kernel-dashboard).
Verifs post-change: build clean, tests pass, aspire doctor pass.

[committed 8edd04c] feat(ui+kernel): kernel-dashboard surface + rolling phases + packaging

Post-commit verifs: build clean, 34 high-sev tests pass, aspire doctor pass.

Packaging: bumped DigitalBrain.Aspire to 0.3.0 with updated description for rolling/UI.
Verifs: Aspire package build clean, tests pass, doctor pass.

Packaging: added 'kernel' 0.3.0 as first-class versioned distributable to MarketplaceSeeds.
Verifs: build, tests pass, doctor pass.

Version consistency: kernel update now uses 0.3.0 to match seeded pack.
Verifs: build/tests/doctor green.

Post-commit verifs (after 5944698): build clean, 39 high-sev tests pass, aspire doctor pass.
Next: expand Reqnroll for rolling phases + UI surfaces, more declarative UI, use seeded kernel pack.

Test expansion: added KernelDashboard to UiSurface samples and contract tests.
Verifs: build, specific test pass (18+).
Next per doc: more Reqnroll for phases, coverage report review, full Aspire demo.

Test: expanded Reqnroll with UiSurface for kernel update scenario.
Verifs: 10 pass.

[committed 8b6f72e] test: expand Reqnroll with UiSurface for kernel update

Verifs: 10 pass, doctor pass.
Expanded coverage for UI surfaces in update flows.

Continued: update kernel self-update to use seeded pack version/desc from MarketplaceSeeds.
Verifs: build clean, tests pass.
## 2026-06-26: proper OTEL for GenAI (neuron + LLM)

Changes under brain/:
* NeuroOSPrototype.ServiceDefaults/Extensions.cs: registered .AddSource("DigitalBrain.Neuron") + .AddMeter("DigitalBrain.Neuron") in ConfigureOpenTelemetry.
* DigitalBrain.Kernel/Llm/DigitalBrainChat.cs: wrapped Ollama and AzureOpenAI IChatClient registrations with new ChatClientBuilder(raw).UseOpenTelemetry(sourceName: "DigitalBrain.Neuron").Build() before AddChatClient.
* DigitalBrain.Kernel/Neuron.cs: added using for Diagnostics; introduced public static NeuronInstrumentation (ActivitySource + Meter + db.synapses.in/out + db.handle.duration); instrumented DeliverAsync (StartActivity + tags + handleStopwatch + records) and FireAsync (out counter).

All direct IChatClient sites now get GenAI-instrumented clients. Neuron dispatch/fire now emit traceable spans + db.* metrics. LLM calls produce full gen_ai.* semantic conv spans (nested under neuron traces) matching Projects/final + Projects/ino/IAW.

Commit: b9659e1 feat(brain): proper OpenTelemetry for GenAI + neuron instrumentation

Verification performed after edits:
- Context7 used for Microsoft.Extensions.AI (ChatClientBuilder.UseOpenTelemetry + gen_ai emission), OpenTelemetry .NET (AddSource/AddMeter), Aspire (ServiceDefaults/ConfigureOpenTelemetry) — before any edit.
- dotnet build (kernel + AppHost + tests) clean.
- High-severity targeted tests (ChatClientRegistration + neuron/llm/ino/company/ checkpoint filters): 68+ passed, 0 failures.
- aspire__doctor (MCP): 4/4 pass.
- aspire MCP tools (select, list_resources, doctor, rebuilds via execute_resource_command) used.
- Relative paths only. Self-explanatory names throughout (ollamaClient, chatClient, handleStopwatch, NeuronInstrumentation.*). No vacuous summaries or default comments added. Latest central packages respected.

Refs: research of Projects/ino/IAW (AgentTelemetry + UseOpenTelemetry in LlmRegistration) + Projects/final (NeuronInstrumentation + LlmNeuron wrapping).

Continued after commit...
